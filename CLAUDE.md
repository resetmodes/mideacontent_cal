# CLAUDE.md — 매체 스펙 라이브러리 + 매체 일정 캘린더 (media-spec-library)

> **계정 이관 시: `docs/handover-account-move.md`** — 연동 자격증명·SQL 확인표·미완료 항목.
> **작업 전 필독: `PLAYBOOK.md`** — 작업 절차·금지선·파일 지도·검증 도구('26.7 하네스).
> 코드 수정 후에는 반드시 `npm run verify` (파서·가드 테스트 + 빌드) 통과 확인.

## 프로젝트 목적
현대백화점 미디어콘텐츠팀의 매체 운영 허브. 상단 탭 2개:
1. **매체 스펙** — 전 매체의 소재 규격, 납기, 진행 프로세스, 심의·금지사항, 담당자 조회.
   지점 담당자·대행사에 링크 하나로 전달. 등록·수정은 데이터 파일 직접 편집.
2. **매체 캘린더** ('26.7 추가) — 팀 내 광고 집행 일정 공유. 빠른 입력 한 줄로 등록
   ("12/20 크리스마스 인스타 릴스 #크리스마스"), 클릭해서 수정·삭제, 캠페인(#태그)별 묶어 보기.

## 캘린더 구조 ('26.7)
- **v2 리디자인 전면 적용 ('26.7.29 — 사용자 프로토타입 승인 후)**: ① 셸 = 상단 탭 바 →
  **좌측 플로팅 글래스 사이드바**(App.jsx `.shell`/`.side` — 브랜드·탭·세션(알림/이메일/로그아웃),
  모바일 ≤840px는 상단 가로 스크롤 바 폴백). 미러는 상단 중앙 글래스 필 바(MirrorApp `.mini-tabs`)
  ② 캘린더 = **좌측 패널**(이번달 미니 먼스 `MiniMonth` + 매체/유형 필터 세로 리스트) +
  **"크게 보기" 토글**(패널 숨김 + 폭 1520px 확장, localStorage `calBig` 기억, 검색 중·미러는
  기존 구조 유지 — 미러 와이드 1560px 불변) ③ 매체 스펙 = 데스크톱(내부·미러) **마스터-디테일**
  (좌측 목록 rail + 우측 상세 `MediaItem forceOpen` — 외부 뷰·모바일은 기존 아코디언 유지,
  딥링크 specFocus는 rail 선택으로 연결) ④ 채널 덱 = 다크(#16181C)·형광 → **딥 그린 그라데이션
  무대 + 골드(--gold) 액센트** ⑤ 영문 eyebrow 8개 페이지 전면 제거. 디자인 토큰은 아래
  "디자인 규칙 (v2)" 참조
- **v2 2차 ('26.7.29 — 홈 시안 정합·전 페이지 공통화)**: ⑥ **사이트명 "미디어콘텐츠룸"
  (약칭 미콘룸)** — '26.7.29 사용자 확정. 사이드바 브랜드 + index.html title
  (허브·코어는 사용자 금지어) ⑦ 홈 = **인사 헤드라인**("M월 D일 X요일, 오늘 게시 N건 · 촬영 N건이 있습니다"
  home-greet) + KPI 글래스 카드 4개 + **2열**(home-cols — 좌: 오늘의 팀(근태)/오늘·내일
  게시(매체·촬영 — **팀과 패널 분리**, 사용자 지시)/업무·마감/주요 콘텐츠/이번 주 촬영,
  우: 하이라이트·채널 이슈). 전 섹션 = home-sec 글래스 패널 + **설명줄(home-sec-d)** —
  "이 블록이 뭘 말하는지" 명시 ⑧ **공통 컴포넌트 언어**: `.mon-stat` = KPI 글래스 카드,
  `.mon-scroll` = 글래스 테이블 패널, `.group-label` = 잉크 17.5px 섹션 제목 — 홈·모니터링·
  RMN·정산·어드민이 CSS 레벨에서 자동 공유 ⑨ **타이포 전면 확대**: 본문 15.5px,
  KPI 값 최대 46px, 테이블 14.5px, 사이드바 14.5px (폰트는 document.fonts 실검증 —
  Wanted Sans Variable loaded 확인)
- **v2 3차 ('26.7.29 — 대시보드 확장, 시안 승인 후)**: ⑩ **공용 차트 `src/Charts.jsx`**
  (의존성 0 인라인 SVG — Spark·Donut·HBars·DuoBars·TrendLines·Gauge·Pipeline·Kpi.
  시리즈 = 그린 명도 계단 SERIES 5단, 비교군 = 라일락, 형광 = 최대값 1개 전용,
  이상치는 sqrt 스케일 + 표기 명시) ⑪ **RMN 세그먼트 3분할** — [진행 현황 | 부킹 등록 | 분석],
  **기본 진입 = 진행 현황** (사용자 확정). RmnPage `mode` state (기존 분할일정 setSeg와
  이름 충돌 주의). 등록 = 2열(rmn-form-cols — 좌 폼 / 우 sticky 부킹 요약·선택 기간
  재고(rinv, 이번 부킹 몫 병기)·최근 등록 3건), 상품 카드에 기간 재고 미니 게이지(pc-bar).
  수정 버튼(startEdit)은 자동으로 등록 세그 진입. 분석 = RmnAnalytics(부킹 실데이터 —
  KPI+월별 스파크·월별 매출 올해vs작년·상품 믹스 도넛·이번 달 재고 게이지·상태 파이프라인·
  GA 실적 랭킹, 전부 rows 계산·날조 없음) ⑫ **모니터링 4세그 차트**: 인스타(참여도 sqrt 랭킹·
  그룹 팔로워 도넛·30일 게시·릴스 비중), 유튜브(**구독자 순증 TrendLines** — trend.js 4스냅샷,
  구성 도넛·평균 조회 sqrt 랭킹 — 인스타 순증은 trend에 1스냅샷뿐이라 미구현·팔로워 사고 여파),
  UGC(감정 도넛·주제 랭킹 — 분석분 있을 때만), 타겟APP(월별 노출 막대·구분별 노출 도넛)
- **v2 3차 보정 ('26.7.29 — 사용자 5개 지시)**: ⑬ **overflow-x hidden → clip** (html/body —
  hidden은 스크롤 컨테이너를 만들어 사이드바·캘린더 패널·스펙 rail·RMN 요약의 sticky를
  전부 무력화했음. clip은 클리핑만 하고 sticky 보존 — 재발 주의) ⑭ 본문 폭 .wrap 1240→1400px
  (크게 보기 1600px) ⑮ **차트 시리즈 파스텔 확장** — 그린 단색 계단은 구분이 안 됨:
  SERIES = 그린 #0B4336 · 라일락 #C9A0BE · 세이지 #7FA795 · 샌드 #D9C6A5 · 슬레이트 #9BA8C4
  (Charts.jsx 단일 소스, 랭킹 바는 그린 단색 유지) ⑯ **flat 패널** (.dash-panel.flat) —
  도넛·게이지류는 박스 없이 배경에 띄움 (전부 박스면 답답) ⑰ 곡률 확대: 패널 22px ·
  사이드바 26px · 상세 24px · 모달 20px (레퍼런스 곡률 — 사용자 지시라 "과잉 라운딩 금지"
  원칙의 의도적 예외)
- **폴리시 팩 ('26.7.29 — SnowUI 킷 참고·애플식 절제)**: 색·레이아웃 불변, 반응만 추가 —
  전 입력 공통 포커스 링(그린 보더+헤일로), 버튼 호버 리프트·프레스, 세그·칩 트랜지션,
  커스텀 셀렉트 셰브론, 표 행 호버, 모달 진입 모션, 얇은 스크롤바, ::selection 틴트,
  prefers-reduced-motion 존중. **토스트 `src/lib/toast.js`** — 저장·삭제 시 하단 중앙 필,
  확인 버튼 없이 2.2초 자동 소멸(사용자 확정), 캘린더·RMN·정산 9개 지점 배선.
  모바일: KPI 2열·랭킹 바 폭·차트 여백 보정
- **홈 가독성 보정 ('26.7.29)**: 오늘·내일 그룹 = 좌측 라벨 rail → **칩 헤더**(오늘=그린 /
  내일=그레이) + 그룹 사이 구분선(.home-day.next). 모바일은 **행 2줄 구성** — 1줄 아이콘+제목
  (2줄 클램프·전체 폭), 2줄 D-day·캠페인·기간 메타 (제목이 "도쿄장난감미술…"처럼 잘리던 문제).
  주요 콘텐츠 행은 제목이 주인공 순서로 재배치(캠페인명은 메타 뒤로, 다음 게시 없으면
  캠페인명이 제목 자리)
- **매체 스펙 보정 ('26.7.29)**: ① 데스크톱 rail은 **매체명만** (카테고리·정기·요청 D-day
  설명줄 삭제 — 사용자 지시) ② **모바일(≤840px) v2 카드화** — 아코디언이 v1 가로선 톤으로
  남아 있던 것을 글래스 카드 리스트 + ＋/− 펼침 어포던스로, 검색은 라운드 글래스 필드,
  필터는 알약 칩(선택 시 그린 채움), 공통 가이드도 카드 + 좁은 폭 세로 스택,
  카테고리 표기(m-cat) 숨김 ③ **채널 배지** (`splitMediaName` + `.m-chip`) — "인스타그램 ·
  공식 (the_hyundai)"를 [아이콘 인스타] + "공식"으로 표시. **계정 주소(괄호)는 표시에서 제외**
  하되 data/media.js의 name 원본은 불변 (딥링크·specLink·지표 매핑 키). 괄호 제거는 채널
  항목에만 — "홈페이지 (WEB)"·"고지물 (PMS)"의 괄호는 구분자라 보존 ④ 레퍼런스 썸네일의
  흰 여백 제거 (테두리 삭제 + mix-blend-mode:multiply로 캔버스에 녹임) ⑤ 모바일 목업 축소 —
  세로 폰 목업이 스펙을 밀어내던 문제: 썸네일 84~96px·상단 크롭, 전체는 탭하면 라이트박스
  팀 일정 `#team` / `CalendarPage.jsx`(매체 캘린더 `#calendar`) / `SpecLibrary.jsx` / `MonitorPage.jsx`. 뷰:
  기본(팀용) · `?view=mirror`(타 팀 공유 읽기 전용) · `?view=external`(대행사·지점용
  스펙만, 캘린더·모니터링 완전 숨김). `#spec` / `#monitor` = 탭 딥링크.
  각 탭에 공유 링크 복사 버튼(`src/ShareButton.jsx`)
- **사이트 전체 로그인 락 ('26.7)**: 로그인 게이트를 App.jsx 최상위로 이동 — 탭·뷰 파라미터
  불문하고 세션 없으면 `src/LoginScreen.jsx`만 렌더 (매체 스펙·SNS 모니터링도 예전엔 로그인
  없이 열렸으나 지금은 3탭 전부 잠김). 로그인 후 상단 탭 바 우측에 이메일 + 로그아웃 상시 노출.
  CalendarPage.jsx는 자체 로그인 폼을 갖고 있었으나 제거 — App 레벨 게이트에 위임
- **미러 전용 사이트 ('26.7 구현)**: 로그인 없는 읽기 전용 별도 사이트 — 같은 리포에서
  `VITE_MIRROR=1` 빌드 분기 (`src/MirrorApp.jsx`, main.jsx에서 App 대신 렌더). 캘린더(읽기
  전용)+스펙만, SNS 모니터링·등록 UI 없음. 배포 = Vercel 두 번째 프로젝트(같은 리포 재임포트
  + env `VITE_MIRROR=1`), 캘린더 읽기는 Supabase anon SELECT 정책 필요 — 절차·보안 유의는
  `data/mirror-setup.md`. **사용자 액션 대기: Vercel 프로젝트 생성 + Supabase SQL 1줄**
  (정책 전엔 미러 캘린더 빈 화면이 정상, 쓰기는 계속 차단)

## 매체 스펙 라이브러리 개편 ('26.7 — On·Off 광고 제작 가이드 반영)
- **소스**: "현대백화점 On·Off 광고 제작 가이드" PDF (26p) — 매체별 확정 제작 스펙.
  지면(slot) 스키마 확장: `kind`(이미지/영상/이미지·영상/텍스트) · `img`/`vid`(확장자 분리) ·
  `vlen`(영상 길이) · `vspec`(비트레이트·fps) · `text`(텍스트 소재 규칙) · `rules[]`(준수 사항) ·
  `ref`(레퍼런스 이미지 파일명). 구형 slot(fmt만)은 기존 간단형으로 렌더 (하위호환)
- **레이아웃**: 지면 카드 2단 — 좌측 스펙 표(유형/형식/용량/영상 길이·사양/텍스트, 가로선만) +
  준수사항 리스트, 우측 **레퍼런스 이미지**(가이드 원본 페이지 렌더, 클릭 = 라이트박스 확대).
  상단에 "광고 소재 공통 가이드" 접힘 섹션(media.js COMMON_GUIDE — 텍스트·이미지 작성 원칙)
- **레퍼런스 이미지**: `public/media-ref/guide-NN.jpg` — PDF 페이지에서 **좌측 Design Guide
  비주얼(목업·현장 사진)만 크롭** (스펙 표 중복 노출 지적으로 장표 통짜 → 목업 크롭 교체, '26.7).
  **타겟형 10종 지면 목업 ('26.7.28)**: `ta-{매체}-{지면}.jpg` 25장 — '26.2 타겟형 가이드라인
  PDF 제작가이드 10장에서 자동 크롭(150dpi 렌더 → 슬라이드 배경색 대비로 목업 열 검출 →
  가장 긴 연속 세로 블록 = 목업, 딤 처리 화면도 잡히게 임계 12·간격 60px 허용 → 흰 여백
  패딩). 규격 라벨·설명 텍스트는 제외(스펙 표와 중복). 슬롯 `ref`로 연결 — 지면 카드 우측에
  해당 지면 목업 (키즈노트 포토디테일은 가이드에 목업 없음).
  재생성: pdftoppm 150dpi 렌더 → x 4.5~56.5% · y 20~98.5% 크롭 → 흰 여백 자동 트림.
  파일 실재는 test-data 7이 검증. **전 매체 일관 카드**: 타겟형·인스타·유튜브·LCD 등 기존
  fmt 필드도 kind/img/vid/vlen으로 전환 완료. 매체 대표 비주얼(`media.visual` — 채널 캡처·
  목업)은 **본문 우측 열 고정 배치** ('26.7 레이아웃 변경 — 상단 통짜가 스펙을 밀어내
  사이니지 지면 카드와 같은 우측 배치로, 모바일은 본문 위 유지. 새 매체도 media.visual에
  파일명 한 줄이면 자동 우측). 자산 확보 시 파일 넣고 한 줄 지정.
  인스타 4계정 목업 적용 완료('26.7.27 — 공식 갱신 + 도시매뉴얼·에딧뎁트·와지트,
  insta-*.jpg. 사용자 제공 목업을 흰 배경 합성·트림·높이 960 통일).
  유튜브 4채널 목업 + 버스TV 실사 적용('26.7.28 — yt-*.jpg 공식·룸넘버·이야호·와지트,
  bus-tv.jpg. 같은 처리 파이프라인). **와지트 통합 ('26.7.28 사용자 지시)**: 인스타그램 ·
  와지트 + 와지트 2개 항목 → "인스타+유튜브 · 와지트 (wazit_wine · WAZIT)" 한 항목 —
  IG·YT 지면을 '인스타 ·'/'유튜브 ·' 접두로 병합, visual 배열(2장 세로 스택, SpecLibrary
  media-visual-col — 앞으로 어느 매체든 visual:[a,b]로 복수 가능), specLink '유튜브|와지트' 갱신.
  **계정 표기 통일 ('26.7.27)**: 언드 미디어 전 계정 = "채널 · 세부 (계정)" 패턴 —
  인스타그램 · 공식 (the_hyundai) / 유튜브 · 공식 (THE HYUNDAI) 등 (구 "인스타그램 대표계정"·
  "유튜브 (현대백화점 THE HYUNDAI)" 표기 정리). specLink.js 매핑도 세부 계정까지 확장
  (인스타 도시, 유튜브 룸넘버·이야호 → 각 스펙 항목 딥링크). 인스타 지면에서 정방형
  캐러셀(1080×1080) 삭제('26.7.27 사용자 지시 — 세로형 1080×1350만 유지).
  **언드 미디어 서브 계정 등재 ('26.7)**: 인스타 대표계정 외 도시매뉴얼(dosi.manual)·
  에딧뎁트(edit.dept)·와지트(wazit_wine), 유튜브 룸넘버·이야호(yiyaho_studio) 추가.
  제작 규격은 계정 무관 동일이라 `IG_SLOTS`/`YT_SLOTS` 상수 단일 소스로 재사용 —
  포지셔닝·지표는 미확보라 지어내지 않고 "모니터링 탭 참조" 안내 (확보 시 target·extra 채움).
  **채널 캡처 → 아이폰 목업 합성**: `scripts/media/iphone-mock.py` (PIL) — 스크린샷만 주면
  베젤·다이나믹 아일랜드·라운드 스크린 프레임 자동 합성 (사용자가 목업 제작 불필요).
  실행: `python scripts/media/iphone-mock.py <shot.png> public/media-ref/<name>.jpg`
  (캡처에 iOS 상태바 있으면 상단 ~150px 크롭 + 흰 여백 ~130px 패딩 후 합성해야 아일랜드가
  콘텐츠 안 가림). 인스타 대표계정 목업 `insta-thehyundai.jpg` 적용 완료 ('26.7 첫 사례)
- **탭 바 1줄 개편 ('26.7 — 2차)**: 데스크톱·모바일 공통 — 탭이 상위 1줄(줄바꿈 없음,
  넘치면 가로 스크롤), 세션 정보(알림·이메일·로그아웃)는 **하위 별도 줄**. 정산 탭 추가로
  데스크톱도 2줄로 겹치던 것 해소. App.jsx tabs-scroll 래퍼 + tabs-session order:2
- **RMN 캠페인 제안서 메이커 가시성**: 접힘이지만 그린 CTA(테두리+＋아이콘, 열면 그린 채움
  헤더)로 노출 — `.rmn-propmaker`, ta-office 공용 스타일과 분리 (완료·취소·어드민 아코디언 불변)
- **PDF 기준 데이터 반영**: 앱(APP) 지면 전면 재작성 — 스플래시 하단형/전면형(1182×2560),
  메인 팝업 720×512(구 "규격 확인 중" 확정), 메인(프리즘) 750×750 영상 15초, 헤드라인 400×400,
  **메인 하단 배너 650×242 (구 썸네일 하단 325×121 대체 — 대장과 상이했음)**, 이벤트 메뉴(텍스트),
  PUSH 500×250, VIP 배너 656×218(신규). 홈페이지(WEB)에 팝업 MO/PC·하단(MO)·우측(PC) 배너 4종
  추가. 사이니지: 판교 2층·키오스크 더현서/판교 신규, 기존 위치 포맷·비트레이트 확정
  (verified true 승격 — 가이드 미기재 위치 5곳은 "가안" note 유지). 구좌·Shopping info는
  아이랩 확인 대기 유지

## 미디어 바이럴 라운지 ('26.8 — 실구축, 3차에 전사 공개)
- `#lounge` 탭, `src/LoungePage.jsx`(React.lazy) — 전사 타 팀이 자사 매체 광고·바이럴을
  신청하는 창구. **3차 ('26.8.1 사용자 지시 "홍보효과 + 투명한 집행") = 전사 공개**:
  미러 사이트(무로그인)에 "바이럴 라운지" 탭(`LoungePublic` = 신청 폼 + **진행 현황 보드**),
  본 사이트 라운지 탭은 팀 로그인 전체로 오픈(접수 관리, 구 ADMIN 게이트 해제) +
  관리함 헤더 "신청 링크 복사"(MIRROR_URL#lounge). **공개 보드 = `lounge_board` 뷰**
  (이메일·문안 상세·브랜드 소스·반려 사유·메모 제외 — anon은 이 뷰만 SELECT, 원본
  테이블 anon SELECT 없음. **뷰 컬럼 추가 = 곧 전사 공개**, test-data 15가 컬럼 감시).
  세그: 본 사이트 [접수 관리(기본), 신청 접수] / 미러 [신청 접수(기본), 진행 현황].
  RMN(유료 판매)은 대상 아님
- **공개 보드 카드형 ('26.8.1 — 사용자 지시 "낯선 사람에게 활성화가 보이게")**: 표 →
  **KPI 히어로 3장**(올해 접수·진행 중·게시 완료, mon-stat 재사용) + **진행 중 카드**
  (매체 브랜드 아이콘 칩 ChannelIcon·최근 7일 NEW 형광 배지·게시 D-day) +
  **"최근 게시 완료" 분리 섹션**(집행 결과 이미지 = 확정 시 연결한 일정의 첨부 재사용,
  event-images 공개 버킷이라 미러 표시 가능 — 뷰에 linked_events 포함, 일정은 미러
  캘린더 anon SELECT로 이미 공개라 id 노출은 추가 공개 아님. 이미지 404는 영역 숨김).
  접수 관리함(팀용)은 표 유지. 반려 건은 카드에서 제외하고 하단에 건수만.
  **완료 카드 클릭 = 상세 모달** (BoardDetail, ModalShell 재사용 — 결과 이미지 전체 그리드 +
  매체·목적·기간·담당. 공개 뷰 컬럼만, 문안·소재 원본은 계속 비공개).
  오픈 초기 활성 인상은 팀즈 이력 소급 입력으로 채우는 게 전제.
  **anon 접수 수리 ('26.8.1 점검)**: anon은 원본 SELECT 정책이 없어 return=representation이
  INSERT째 거부됨(RETURNING이 SELECT 권한 요구) — 무로그인이면 return=minimal + 로컬 구성.
  팀즈 메시지 자동 인입 워크플로는 검토 후 사용자 기각 ('26.8.1 — 팀즈 습관 영구화 역효과)
- **결과 회신 카드 ('26.8.1)**: 관리함 게시 완료 건 "결과 회신 복사" — 신청 부서 팀즈
  회신용 텍스트(lounge.js resultReply). 성과 = campaignPerf.buildCampaignPerf 재사용
  (linked_events 우선, 없으면 캠페인 태그로 일정 수집 — 수치 있는 매체만 싣고 나머지는
  "집계 확인 중", 추정 없음) + 미러 보드 링크 + 담당. SNS 수집분은 버튼 클릭 시에만
  동적 import. 신청 → 게시 → 결과 회신 루프의 마지막 조각
- **팀즈 이력 소급 입력 ('26.8.1)**: `data/lounge-seed.sql` — 팀즈 "바이럴 요청" 채널
  실제 신청 16건 (**사용자 액션: SQL 1회, setup.md 14-1장**, 재실행 안전).
  **지어낸 값 없음** — 이메일(원문에 없음)·담당(배정 이력 미상)·목적(우리 필드)은 비움,
  상태는 희망 게시 일정 기준 자동 판정(종료=게시 완료 / 진행·임박=확정 / 9월·소재 미정=협의)
  이라 실제와 다르면 관리함에서 수정. 첨부 파일은 미수령 원칙대로 파일명만 memo에.
  created_at은 우리 시스템 등록일(8/1)이고 팀즈 순서만 분 단위로 유지.
  test-data 16이 **열 개수 대조**(값 1개만 어긋나도 Supabase에서 통째 실패)·매체 id·
  타겟 앱·구좌·상태·이메일 미날조를 회귀 감시
- **홈 라운지 위젯 ('26.8.1 — 기본 숨김)**: HomePage LoungeWidget (미배정 N건, 이번 주
  게시 N건 → 라운지 탭 이동, 정산 배지 패턴). **config.js `LOUNGE_HOME_WIDGET = false`가
  게이트** — 실험 종료 후 true 한 줄이면 홈에 노출 (사용자 지시로 현재 비노출)
- **거버넌스**: 신청자 무계정 원칙 유지 — `media_requests`는 **anon INSERT만** 열고
  SELECT는 로그인 전용(신청자는 남의 신청 못 봄), 수정·삭제는 team_writers.
  계획안 등 기밀 문서는 파일로 안 받음(보유 체크만 → 담당 배정 후 팀즈 개별 공유),
  푸시 엑셀 타겟(고객번호)도 미수령("담당자에게 별도 전달" 안내만). 브랜드 소스는
  **lounge-src 비공개 버킷**(미공개 행사 로고 가능성 — event-images와 달리 비공개,
  관리함 열람은 토큰 fetch 후 blob URL). **사용자 액션: data/lounge-setup.sql 1회
  (setup.md 14장)** — 미실행 시 라운지 탭 안내 문구만
- **규칙 단일 소스 `src/data/lounge.js`** — "APP 광고 가이드 v0.4"(커뮤니케이션팀 전달 양식,
  DRM 엑셀이라 스크린샷으로 수령)를 코드로: 구좌 4종(Shopping info 750×750 제목 2줄×11자
  말줄임 / 하단 배너 650×242 제목 관리용 / 메인팝업 720×512 최대 1주 권장·노출대상 택1 /
  푸시 500×250 선택·제목 30자·내용 60자(줄당 30자)·**이모지 불가(접수 차단)**·유형 12종),
  **리드타임 3단**(완성 소재 +3 / 이미지 제작 +5 / 페이지 제작 +10 영업일 — holidays.js
  기반 영업일 계산으로 희망 게시일과 대조, 경고만 하고 접수는 안 막음), 이동페이지 택1
  (프리즘 행사카드 = 카드번호·등록지점·카테고리 + **승인 완료 체크**(미승인 세팅 불가) /
  APP 메뉴 8종 / URL), 노출·발송 대상(자사카드 등급 다중선택·H.point·엑셀 타겟),
  기본 담당 = 백화점APP 정 이수정 선임, 부 백채은 선임·하지훈 책임 (가이드 명시)
- **신청 폼**: 좌 폼 + 우 sticky 목업 — 구좌 선택 시 폰 프레임에 해당 구좌 위치로 게재
  예시(shopping 정방형 카드 / banner 하단 스트립 / popup 바텀시트 / push 알림 카드),
  브랜드 소스 올리면 합성 + **비율 5% 이탈 경고**, 푸시는 제목·내용 실시간 반영.
  글자수 카운터, 리드타임 자동 판정, 이동페이지 조건부 필드. 접수 완료 = 접수번호
  MR-YYMMDD-NN. 게시일 필수 + 시간 선택(푸시만 발송 시간 필수)
- **접수 관리함**: 미배정·협의 중·이번 주 게시 요약 칩(클릭 필터) + 목록 + 행 상세.
  담당 배정 = 기본 담당 제안 + TEAM 전체 수기 선택, 파이프라인 접수→담당 배정(owner
  유무 파생)→협의→확정→게시 완료 (+반려 = 사유 입력 2단계). **"확정, 캘린더 등록"** =
  매체별 createEvent 동시 등록(아젠다명 공백 제거 = 캠페인 태그, 백화점APP 구좌→sub
  푸쉬/팝업 매핑, 등록 id는 linked_events 보관). 추천 요청 건은 관리함에서 매체 지정 후
  확정. **"전달 양식으로 복사"** = 가이드 요청 템플릿 순서의 텍스트(lounge.js transferText)
  클립보드 — 담당자가 앱 어드민 등록·커뮤니케이션팀 전달에 그대로 사용.
  test-data 13이 영업일·글자수·이모지·규격·전달 양식 회귀 감시
- 이 가이드로 media.js "구좌, Shopping info" 규격 확정(750×750, 구 "규격 확인 중" 해소)
- **2차 ('26.8.1 — 타겟APP + 전달 경로)**: ① **타겟APP 상세** — 앱 다중 선택(channels.js
  세부 11종, 전체 선택 버튼) + 앱별 스펙 요약(리드타임 D-N·지면 규격·주류 검수 주의) +
  우측 목업 = **media.js 타겟형 스펙과 ta-*.jpg 가이드 이미지 재사용** (라운지에 중복 등재
  금지 — lounge.js targetSpec이 media.js 조회, K-Ride만 미등록 표기). 앱 탭·지면 탭 전환,
  비율 검사는 보고 있는 지면 기준. 리드타임 경고는 선택 앱 중 최장 D-N(maxTargetLead,
  앱 이름 병기). **확정 시 캘린더 등록은 선택 앱마다 1건**(1건=1세부 모델, 셀에서 ×N 묶임)
  ② **전달 경로 체크리스트(handoffPlan)** — "이 접수로 전달되는 것 / 팀즈로 따로 전달할
  것" 두 목록을 선택 상태에서 파생(소재 추후·계획안·엑셀 타겟·행사카드 미승인), 폼 하단과
  접수 완료 화면에 동일 표시 ③ **마감 실날짜** — "D-5 영업일"을 bizDeadline 역산으로
  "8/13(목)"처럼 달력 날짜 병기 (폼 리드타임 줄·체크리스트·관리함 소재 마감·전달 양식)
  ④ 접수 완료 화면 **"접수 내용 복사"** (transferText 재사용 — 신청자 보관·팀즈 소통용).
  test-data 14가 스펙 재사용·역산·체크리스트 회귀 감시

## 커뮤니케이션 금지선 ('26.7.28 사용자 지시)
- **팀즈 공식 채널(TEAMS_WEBHOOK_URL)은 공식 알림 전용** — 아침 브리핑 등 확정된 정기
  발송만. **작업 중간 산출물(검증 결과·대조 리포트 등)을 팀 채널로 발송 금지**
  (audit 결과 발송 사고 후 확정 — ga4-collect --audit 모드 자체를 폐기)

## 매체 모니터링 탭 ('26.7 — 구 "SNS 모니터링", 타겟APP 추가되며 개명)
- `src/MonitorPage.jsx` — 인스타그램/유튜브 세그먼트 전환. 캘린더와 동일 톤
  (히어로 타이포 지표 + 가로선 테이블 + 그룹 라벨)
- **타겟APP 실적 세그먼트 ('26.7)**: 캠페인 실적 수기 입력 모니터링 (API 연동 불가 매체).
  데이터는 **Supabase 전용**(targetapp_stats 캠페인 행 + targetapp_media 매체별 누적 스냅샷,
  RLS 로그인 전용·anon 정책 없음) — 실적 수치를 번들 파일에 싣지 않음 (공개 미러 번들
  유출 방지, 내부 전용 '26.7 사용자 결정). src/data/targetapp.js는 매체 구분 메타만
  (test-data 6c가 수치 유입 감시). '26.1~4월 50건 이관 SQL = `data/targetapp-seed.sql`
  (**사용자 액션: Supabase SQL Editor 1회 실행** — setup.md 7장). 미실행 시 안내 문구만.
  화면: KPI 히어로 / 월별 추이 / 매체별 누적(구분 그룹) / 사업소별 아코디언(월 필터 +
  인사이트 노트, note는 사업소 마지막 행에 저장). 신규 입력 = 매월 초 전월분,
  입력 폼은 어드민 페이지(2차)에서 — 그전엔 Supabase Table Editor 직접 입력
- 데이터: `src/data/sns/instagram.js`(자사 계정 요약 + 경쟁사) · `youtube.js`(채널 + 영상) —
  자동 생성 파일, 직접 수정 금지
- **수집 파이프라인 내장 ('26.7 이전 완료)**: `scripts/sns/` — accounts.mjs(계정 메타 단일 소스),
  scrape/clean × 인스타·유튜브. `npm run sns:collect` = 수집+정제 전체.
  `.github/workflows/sns-collect.yml` — 매주 월 06:00 KST 자동('26.7.27 사용자 지정, `0 21 * * 0` — UTC 일요일 주의.
  '26.7 매주 전환, 비용 항목 참조. **sns-collect도 '26.7.27 크론 무발화 사고** — notify.yml과 동일 원인,
  크론 변경은 GitHub 웹에서 본인 커밋으로) + 수동(Run workflow),
  결과를 src/data/sns에 커밋 → Vercel 자동 재배포. APIFY_TOKEN은 GitHub Secret + 로컬 .env
  (.env는 gitignore — 절대 커밋 금지). raw는 data/sns-raw(미추적)
- **고객 게시물 모니터링 ('26.7 추가, '26.7.29 "UGC"에서 개명 — 사용자 지시)**: 모니터링 탭
  세 번째 세그먼트 "고객 게시물" — 고객·인플루언서가
  해시태그로 올린 게시물을 수집·분석 (`scripts/sns/ugc-config.mjs` 단일 소스 — '26.7.29
  현재 8개: 더현대서울·더현대·현대백화점·더현대대구·현대프리미엄아울렛·현백·커넥트현대·더현대팝업,
  태그 1개 추가 = 회당 약 $0.16).
  파이프라인: scrape-ugc.mjs(태그 게시물 + 반응 상위 30명 프로필 조회) → clean-ugc.mjs
  (중복 제거·자사 계정 제외·광고/협찬 플래그·인플루언서 판정 = 팔로워 1만+ +
  **Claude(claude-opus-4-8)로 게시물별 감정(긍정/중립/부정)·주제 분류 + 주간 동향 요약**)
  → src/data/sns/ugc.js. `npm run ugc:collect`. 워크플로에서 계정 수집과 분리 스텝
  (continue-on-error — UGC 실패해도 계정 데이터는 커밋). **ANTHROPIC_API_KEY**
  (GitHub Secret + 로컬 .env) 없으면 감정·주제·요약만 빠지고 정량 지표는 정상 저장.
  첫 수집 전(UGC=null)엔 세그먼트 숨김. 빈 결과 가드 장착(test-guards 회귀 감시).
  Claude 분석 비용 ≈ $1/회 내외(캡션 량에 따라 변동)
- 계정 그룹 ('26.7 변경): 본사(본계정·도시메뉴얼·에딧뎁트·와지트) / 사업소(점포+신춘자, 아울렛 제외) /
  아울렛(5개점 별도 분리) / 콘텐츠·IP / 해외 (+경쟁사 별도 섹션). 계정 추가·이동 =
  scripts/sns/accounts.mjs 수정 (IG_GROUP_ORDER와 MonitorPage.jsx의 동일 상수 함께 갱신)
- 에딧뎁트(edit.dept)·와지트(wazit_wine) 첫 수집 완료('26.7.7) — 대시보드에 표시됨
- **수집·지표·휴면 기준 통일 ('26.7 확정)**: 전부 "최근 1개월". 수집 윈도우 1 month,
  지표 집계 1개월, 휴면 = 30일+ 미게시. 최근 1개월 게시 0건인 계정은 raw가 비는데,
  clean-instagram.mjs가 지표 없이 "휴면"으로 계정을 유지함 (대시보드에서 사라지지 않게)
- **팔로워 필드 누락 사고 ('26.7.15~ → 7.28 수리)**: Apify instagram-scraper가 post 아이템에
  followersCount를 안 실어줘 7/15·7/20 수집분 전 계정 팔로워·참여/1k가 null로 저장됨.
  조치: clean-instagram.mjs가 ① 후보 필드명(followersCount/ownerFollowersCount/
  userFollowersCount)을 전 아이템에서 탐색 ② 그래도 없으면 **직전 수집값 유지**(휴면 포함).
  현재 instagram.js는 trend 7/7 스냅샷 값으로 복원 완료 — 다음 수집에서 필드가 돌아오면
  자동 갱신, 계속 없으면 7/7 값이 유지됨 (신규 계정은 값 없음)
- **비용 ('26.7 매주 전환)**: Apify는 결과 1건당 과금 — 인스타 $2.70/1,000건, 유튜브 $0.50/1,000건.
  1회 수집 ≈ 계정(인스타 ~600건 + 유튜브 151건) $1.7 + UGC 해시태그(최대 300건+프로필 30건) ~$0.8
  ≈ **$2.5/회, 매주면 월 ~$11** + Claude 분석 ~$1/회 — **Apify Starter($29/월) 결제 전제**
  ('26.7 사용자 결정). 무료 플랜($5/월)으로 돌아가면 격주 게이트 복원 필요(git 이력 참조).
  **cron 주의**: 날짜+요일을 함께 쓰면(`1-7,15-21 * 1`) 표준 OR 규칙으로
  "해당 날짜 매일 + 모든 월요일"(월 ~16회)이 된다 — '26.7 최종 검토에서 발견
- CI 실패 시 Apify 실행(4단계)은 성공해도 커밋·push(5단계)만 실패하는 경우 있음 — 과금은
  이미 발생했는데 데이터는 저장 안 된 상태. sns-collect.yml의 push 재시도(rebase 3회)로
  향후 레이스 컨디션은 방지되지만, 실패 시 재실행하면 다시 과금됨 — 실행 전 Actions 로그로
  실패 지점 먼저 확인 권장
- **빈 결과 가드 ('26.7 사고 후 추가)**: Apify 월 한도 초과(403 actor-disabled) 시 유튜브
  수집이 0건이 됐는데 clean 스크립트가 그대로 저장해 기존 데이터를 빈 값으로 덮어쓴 사고
  발생('26.7.7) → 복구 완료. 이후 clean-youtube/instagram 모두 실수집 0건이면 기존 파일 보존하고
  저장 스킵. **'26.7 현재 Apify 무료 크레딧 당월 소진** — 다음 결제 주기 리셋 전까지 수집 불가
  (수동 재실행해도 403), 리셋 후 격주 스케줄 정상화
- **carry-forward ('26.7 사고 2 후 추가)**: 크레딧이 수집 "도중" 끊기면 뒷순서 계정 raw가 없어
  대시보드에서 계정째 사라지는 사고 발생('26.7.7 — 인스타 26계정 소실 → 직전 데이터로 복구 완료).
  이후 clean 스크립트는 수집 실패 계정·채널을 직전 수집값으로 유지 (지표만 이전 값, 계정은
  절대 안 사라짐). scripts/test-guards.mjs가 두 사고 모두 회귀 감시
- 표기 원칙: 좋아요 비공개 계정은 "비공개", 계정 간 비교는 참여/1k(팔로워 1천 명당 반응) 기준,
  휴면(60일+ 미게시) 계정은 회색 + 휴면 플래그
- hyundai-monitor(구 리포)는 이제 수집 불필요 — 파이프라인이 이 리포로 이전됨
- **채널 성과 덱 ('26.7.29 — 가로형 PPT)**: 모니터링 유튜브 세그 → **채널명 클릭** = 채널 덱,
  **"종합 리포트" 버튼** = 4채널 종합 덱 (`src/ChannelDashboard.jsx`). 스크롤 리포트가 아니라
  **16:9 슬라이드를 넘겨 보는 형식** — 좌우 화살표·키보드 ←→·스와이프·하단 도트 점프·전체화면,
  슬라이드 전환/KPI 스태거/막대 자라기 애니메이션(prefers-reduced-motion 시 정지).
  본문 880px을 벗어나 1180px 가로 확보.
  **디자인 = A+C안 ('26.7.29 사용자 선택)**: 무대 전체 잉크(#16181C) 반전 + 슬라이드당
  **히어로 숫자 1개**(최대 150px, 형광 언더바) + **카운트업** + 차트 강화(평균 기준선·증감 라벨).
  형광 #EDFF5B는 강조 전용 원칙대로 **최대값 1개·언더바·진행바·번호에만**, 막대 본체는 흰색 계열.
  인사이트는 border-left 스트립(금지 패턴) 대신 01/02/03 번호 + 가로선. Chart.js 미사용
  (의존성 0 인라인 SVG·DOM 막대).
  **내용 = 원본 리포트 전량 ('26.7.29 사용자 지시 "분석 내용 그대로")**: `src/data/sns/channelReport.js`
  단일 소스 — 종합 KPI·채널 비교·오가닉 광고가치 환산·채널별 지표/월별/TOP5/**인사이트 문장**·
  와지트 인스타 성과와 전환 퍼널·PPL 현황·향후 운영 계획. 시청시간·CTR·수익·광고비는 API로
  못 얻는 값이라 이 파일이 유일 소스 (다음 리포트 때 이 파일만 갱신). 문구의 `**강조**`는 렌더러가 처리.
  채널 덱 = 리포트 슬라이드 + 주간 수집 지표(기간이 달라 슬라이드마다 기준 병기), 와지트 14장.
  carry-forward의 상대 표기("3 months ago")는 월 버킷에서 제외·게시일은 한글 변환.
  주의: 차트 데이터 점(dk-pt)과 네비 도트(dk-dot)는 클래스 분리 필수 (합치면 도트 클릭이 어긋남).
- **유튜브 스튜디오 지표 연동 ('26.7.29 — 사용자 액션 대기)**: `scripts/sns/yt-analytics.mjs`
  (YouTube Analytics API v2, 주간 수집 워크플로에 스텝 추가 — 시크릿 없으면 조용히 스킵).
  **GA4와 달리 유튜브는 서비스 계정 미지원** → 채널 관리 계정이 1회 OAuth 동의 후
  refresh token을 시크릿(`YT_OAUTH_CLIENT_ID`·`_SECRET`·`_REFRESH_TOKEN`)으로 저장.
  **동의 화면을 "테스트" 상태로 두면 refresh token이 7일 만에 만료**되므로 반드시
  "앱 게시"로 프로덕션 전환할 것 (setup 문서에 필수 단계로 명기).
  채널 ID는 accounts.mjs `YT_CHANNELS[].channelId`에 기입. 절차 = `docs/yt-analytics-setup.md`.
  **노출수·노출 클릭률(CTR)은 Analytics API가 제공하지 않음**(스튜디오 전용) — 대시보드에
  표기하지 않고 안내만 (수치 날조 금지). 수익은 수익화 채널 + 별도 스코프 필요.
  **채널 ID 자동 해결 ('26.7.30)**: accounts.mjs url의 @핸들 → Data API `forHandle`로 조회,
  실패 시 `mine=true` 목록에서 customUrl 매칭 폴백. 사람이 채널 ID를 찾아 넣을 필요 없음
  (핸들이 바뀌면 accounts.mjs url만 갱신). **대시보드 = 모니터링 유튜브 세그 상단
  `StudioBoard`** — KPI 4장(총 시청시간·총 조회·구독자 순증·채널 평균 시청) + 월별 시청시간
  막대 + 채널별 시청시간(sqrt) + 시청 지속률 랭킹 + **유입 경로·기기·시청자 연령·성별**
  ('26.7.30 추가 — 수집 쿼리 3개: insightTrafficSourceType, ageGroup×gender viewerPercentage,
  deviceType. 유입 경로 코드는 화면에서 한글 매핑(TRAFFIC_KO), 미매핑 코드는 원문 표기.
  시청자 수가 임계 미달이면 구글이 빈 값을 주므로 섹션째 숨김. 성별은 미상 제외로 합이
  100 미만이라 확인된 시청자 기준으로 환산해 도넛과 범례 숫자를 일치시킴).
  YTA가 null이면 연동 안내 문구만 렌더.
  **채널 덱 슬라이드 2장 추가 ('26.7.30)**: "시청자는 어디서 들어오나"(히어로 = 1위 경로
  비중 + 경로별 막대), "누가 보고 있나"(히어로 = 최다 연령 비중 + 연령 막대, 성별과 기기는
  부제 한 줄). 슬라이드당 히어로 1개 원칙이라 기기 표는 넣지 않음(막대와 겹쳐 실패).
  BarChart에 `delta` 옵션 추가 — 항목 비교(경로·연령)에는 전월 대비 증감이 의미 없어 끔.
  코드 한글 라벨은 `data/sns/ytLabels.js` 단일 소스(모니터링과 덱 공유, 미매핑 코드는 원문).
  덱 세로 챕터 라벨과 이전 버튼이 겹쳐 있던 것도 함께 보정.
  **종합 덱에도 두 장 ('26.7.30)** — `ytLabels.aggregateStudio`가 4채널을 합산(유입·기기는
  조회수 합, 시청자 구성은 채널 평균. API가 규모 가중치를 주지 않아 단순 평균).
  **모니터링 채널 전환 칩 (`.std-chips`)** — 유입 경로와 시청자 구성은 채널마다 성격이
  달라 합산하면 묻히므로 칩으로 개별 분석 (와지트는 검색 48%, 공식은 쇼츠 35% 식으로 갈림).
  선택 채널에 시청자 데이터가 없으면 그 사유를 문장으로 안내.
  **칩이 KPI까지 지배 ('26.7.30 사용자 지시 "전 채널 합계는 큰 필욘 없어")** — 칩을 스튜디오
  헤더 바로 아래(KPI 위)로 올리고 KPI 4장·월별 시청시간·유입·기기·시청자 구성을 전부 선택
  채널 기준으로 계산(StudioBoard `scope`). **기본 진입 = 대표 채널**(YT.channels의 isMain),
  칩 순서는 [채널들, 전 채널 합계]로 합계를 맨 뒤에 둠. 단 **채널별 시청시간·시청 지속률
  두 패널은 비교가 목적이라 의도적으로 전 채널 유지** (설명줄에 "전 채널 비교" 명시).
  평균 시청 KPI의 부제는 선택 채널이면 "길이의 N%", 합계면 "채널 평균"
- **유튜브 제목 한글화 ('26.7)**: 채널 스크레이퍼는 러너 IP(미국) 기준 자동번역 영문 제목을
  줌 → clean-youtube.mjs가 oEmbed(원본 크리에이터 제목, 로케일 무관)로 보정. 비용 0(무료
  공개 엔드포인트), 실패 시 기존 제목 유지(파이프라인 안전), 동시성 6. videoId·thumb(썸네일
  URL: i.ytimg.com/vi/{id}/hqdefault.jpg) 필드도 함께 저장(홈 화면 대비). carry-forward 구
  데이터는 videoId 없어 다음 수집 때 보정됨
- `src/data/channels.js` — 매체 8종(타겟APP·인스타·유튜브·버스광고·백화점APP·카카오톡·아파트LCD·기타)
  + 세부 + 빠른 입력 키워드 맵. 타겟APP 세부 10종은 '26.2 가이드라인 기준
- `src/lib/parse.js` — 빠른 입력 파서 (날짜/기간·매체 키워드·#캠페인·제목). 연도 자동 추정(6개월 룩백).
  날짜 형식: 12/20 · 12.20 · 7월 10일 · 범위(12/20~25, 7월 10일~8월 2일) · 오늘/내일/모레 ('26.7 확장)
  · 상대 주차 ('26.7): "다음주 목요일"·"다다음주 월요일"·"지난주 토요일" + 금주/차주/저번주
  동의어, 요일 필수("다음주"만은 미인식), 주 시작 = 월요일(업무 관례), 명시 날짜가 우선.
  기간형 ('26.7 확장): "다음주 월~수"·"다음주 월요일~수요일" — ~가 경계라 축약 허용,
  "금~월"처럼 시작보다 앞 요일은 다음 주로 이월. 단일 축약 ('26.7 확장): "다음주 금"도 인식 —
  요일 글자 뒤에 한글이 붙으면 제외("월간 보고"의 월, "토스"의 토 오인 방지).
  등록/검색 입력 구분 ('26.7): 등록 = 그린 "등록" 라벨+박스 입력, 검색 = 그레이 "검색"
  라벨+밑줄만(io-label, 박스 제거) — 같은 생김새 입력칸 2개 혼동 해소
- 제목 표기 통일 ('26.7 확정): channels.js의 TITLE_ALIASES — 표준 표기는
  **인스타 · 도시 · 카톡 · 유튜브 · APT LCD · APP푸쉬 · APP팝업 · APP · BUS**.
  입력 변형(인스타그램 공식 본계정, 카카오톡, 앱푸쉬, 엡, 엘시디, 버스광고 등)을 전부 치환.
  치환 결과는 재치환 보호(parse.js normalizeTitle) — '앱'→APP, 'LCD'→APT LCD 광역 규칙 안전,
  아파트앱 등은 identity 보호. 새 변형 발견 시 TITLE_ALIASES 한 줄 추가.
  KEYWORDS는 채널 직접 지칭 > 포맷 유추(릴스·쇼츠·푸쉬) 순서 — "유튜브 릴즈"는 유튜브
- 채널 지칭 제목 자동 제거 ('26.7): 채널 인식 시 칩과 중복되는 범용 지칭만 제목에서 삭제
  ("인스타 여름테마" → 칩 인스타 + 제목 "여름테마"). channels.js TITLE_STRIP 기준 —
  세부 지칭(도시·와지트·친구톡·APP푸쉬·아파트앱)과 포맷(릴스·쇼츠)은 셀에 안 보이는
  정보라 유지, 조사 결합("인스타로")은 보존, 전부 지워지면 원제목 유지. 소급 없음
  (기존 등록분 제목은 그대로 — 수정 폼에서 개별 정리 가능)
- 유튜브 세부 4종 ('26.7): 공식 / 와지트 / 이야호 / 룸넘버 — 채널명 언급 시 유튜브로 자동 분류.
  와지트는 기타에서 유튜브로 이동. 필터·채널 라벨도 표기 통일: 카톡, BUS, APT LCD
  (CHANNELS의 id는 저장 호환성 위해 유지 — label만 변경)
- 날짜 클릭 등록 모달에 "한 줄 자동 작성" 입력 ('26.7) — 치면 파싱해서 아래 폼 자동 채움,
  날짜 생략 시 클릭한 날짜 유지. **최근 캠페인 칩 ('26.7)**: 컴팩트 시트·전체 폼 모두
  최근 캠페인을 칩으로 노출 — 클릭 한 번으로 캠페인 지정(재클릭 = 해제).
  촬영/업로드 병기 파싱 수정 ('26.7): "7.15 촬영 7.25 현장 스케치"처럼 업로드 라벨이
  없어도 두 번째 날짜를 업로드일로 인식 (이전엔 제목에 "7.25"가 남던 버그). 매체 스펙 상단 데이터 출처 안내문은 삭제됨
- `src/lib/store.js` — 저장 어댑터: `src/config.js`에 Supabase 키 입력 시 팀 공유 DB,
  비어 있으면 localStorage 폴백. 설정 절차는 `data/supabase-setup.md`
- 캠페인 뷰 ('26.7 기준 변경): 최근 캠페인(마지막 게시가 최근 1개월 이내·진행·예정) = 펼침 /
  지난 캠페인(마지막 게시 후 1개월 경과) = 자동 보관(접힘) — 종료 즉시 보관하던 이전 기준은
  막 끝난 캠페인이 바로 묻히는 문제로 교체. 삭제 대신 보관 = 보고용 이력 보존.
  **캠페인 뷰 = 제목형 네비 + 월간 캘린더 ('26.7)**: 캠페인 세그 최상단에 큰 제목 네비
  `‹ #캠페인명 ›`(CampNav — 좌우 화살표로 [전체→캠페인들] 순환) + 아래 작은 선택 칩,
  그 밑에 MonthGrid(캠페인 태그 붙은 일정만, 미지정 제외) + 월 네비, 맨 아래 상세 블록.
  화살표·칩·블록 이름(camp-name-btn) 어느 것으로도 선택 = 그 캠페인만 캘린더 표시(campFocus),
  상세도 그 블록만. 선택 캠페인이 사라지면 자동 전체 복귀. MonthGrid는 CampaignView 안으로
  이동(cursor·onMove 등 전달). 월간 세그·팀/촬영/미러 뷰는 불변
- **캠페인 통합 성과 ('26.7.30)**: 캠페인을 하나 선택했을 때만 월간 그리드와 일정 블록 사이에
  성과 블록(`CalendarPage CampaignPerf` + `src/lib/campaignPerf.js`) — 요약 헤더(기간·건수·
  매체 수·진행 상태) + KPI 4장(총 노출·총 반응·집계된 매체·집계 대기) + 매체별 성과 표 +
  매체별 노출 막대 + 반응 구성 도넛 + 콘텐츠별 실적. **매체마다 집계 출처가 다른 게 핵심**
  (인스타·유튜브 = 주간 수집, 타겟APP = 어드민 실적 입력, 나머지 = 미집계)이라
  `PERF_SOURCE`로 행마다 출처 배지를 달고 값 없는 매체는 "집계 확인 중"으로 남긴다
  (추정·환산 없음). **매칭 규칙**: SNS는 `perf.js matchContent`(모달 후보 표시와 같은 판정)에서
  **일정 1건당 게시물 1건** — 담당자 확정(perfUrl) > 제목 대조 자동 확정 > 날짜 근사 순서.
  날짜로만 고른 건은 행 설명에 "날짜만으로 추정한 건 N건", 콘텐츠 행에 "날짜 추정" 배지.
  타겟APP은 **이름과 시기 둘 다** 맞아야 매칭 ('26.7.30 강화 — 이름만 보면 다른 달 실적이
  붙는다): ① 완전일치이거나 대장 표기가 캠페인 태그를 통째로 품을 것(태그 4자 이상,
  **역방향은 불인정** — 대장의 짧은 이름이 긴 태그에 우연히 걸리는 과매칭) ② 대장 행의
  연월이 그 캠페인 타겟APP 일정이 걸치는 달(`spanMonths`) 안일 것. 매칭된 대장 표기는
  행에 병기해 사람이 검증 가능하게. 유튜브는 좋아요·댓글을 수집하지 않아
  반응이 항상 빈 값 — 0으로 채우지 않는다. 타겟APP 실적은 Supabase 전용이라 캠페인을
  처음 열 때 한 번만 불러 모듈 레벨 캐시. **내부 뷰 전용** (미러·외부·촬영 탭은 `perf` 미전달).
  **성과 표의 매체 행은 그 캠페인에 등록된 일정에서만 나온다** — 일정에 없는 매체가 표에
  뜨는 일은 구조상 불가능하고, test-data 11이 이를 회귀 감시한다 (표에 보이면 일정 데이터
  자체에 그 매체가 등록돼 있는 것).
  RMN 매출은 넣지 않음 — 외부 광고주 판매 도메인이라 자사 캠페인 성과와 성격이 다름.
  백화점APP 자사 집행분이 GA4 `ep_ad_agency`에 잡히는지 확인되면 `PERF_SOURCE`에 한 줄 추가
- **월별 조회수 추이 = 꺾은선 ('26.7.30 사용자 지시 "막대 말고 선형, 추이가 확실히 보이게")**:
  덱의 조회수 막대 3곳(종합 개월 차별 비교, 채널별 월별, 스튜디오 API 월별)을
  `ChannelDashboard TrendChart`로 교체. **선은 SVG, 점과 값 라벨은 HTML로 겹쳐 올린다** —
  viewBox를 가로로 늘이면 원이 타원이 되고 글자가 찌그러지기 때문.
  **주의: 늘인 viewBox에서 stroke-dasharray로 그려지는 연출을 하면 점선처럼 끊긴다**
  (non-scaling-stroke와 pathLength가 충돌) — 판 전체를 좌에서 우로 훑는 clip-path 연출로 대체.
  값 라벨은 한 채널이면 전 지점, 여러 채널이면 선 끝에만(겹침 방지).
  **종합 비교는 제곱근 눈금** — 채널 규모가 100배 벌어져 작은 채널이 바닥에 눌린다
  (단위 줄에 "세로축 제곱근 눈금" 명시). 선 색은 어두운 무대에서 구분되는 4색
  (흰색, 라일락 #DDA3C6, 민트 #8FD3B6, 샌드 #E8CE96) — 공식 라일락 #F0E4ED은 흰 선과
  거의 붙어 보여 차트용 톤을 따로 씀. **모바일 덱 무대 높이 4:3 → 화면 높이 기준**
  (폰 세로에서 292px밖에 안 돼 차트가 무대를 넘었음) + 긴 슬라이드는 세로 스크롤 허용
- **사이트 전수 조사 ('26.7.30 사용자 지시)**: 탭 9개 × 폭 4개 × 공유 뷰를 훑어 아래를 수리.
  재현·회귀 감시는 **`npm run audit`**(+`audit:long`) 하네스로 남김 — `scripts/audit-ui.mjs`
  ① **정산 탭 흰 화면** — `FILE_SLOTS[row.stype]`가 미지의 유형에서 undefined가 되어
  `.map` 크래시. 리액트는 렌더 예외 시 트리 전체를 버리므로 **한 행이 사이트 전체를 죽였다**.
  옵셔널 처리 + **`src/ErrorBoundary.jsx`로 탭 본문 격리**(탭 이동 시 자동 복구)
  ② **모달 9곳이 Esc로 안 닫히고 배경이 같이 스크롤됐음** — `src/ModalShell.jsx` +
  `src/lib/useModal.js`로 통합. **스택 구조**라 모달 안 라이트박스에서 Esc를 눌러도 맨 위
  하나만 닫힌다. 배경 잠금은 `documentElement.overflowY`만 건드림(축약형은 overflow-x:clip을 지움)
  ③ **클립보드 실패 시 window.prompt** — 홈 화면에 설치한 PWA와 일부 인앱 브라우저가 prompt를
  막아 아무 일도 안 일어났다. `src/lib/clipboard.js` 공용 헬퍼(execCommand 폴백 + 토스트 안내)로 교체
  ④ **RMN 캠페인 행이 긴 광고주명에서 넘침** — 이름에 말줄임, 모바일 화살표 같은 줄 유지,
  버튼 글자 세로 쪼개짐(`청 약 서`) 방지 ⑤ 유튜브 썸네일 404 시 깨진 이미지 아이콘 → onError 숨김
  ⑥ **`npm run smoke`가 '26.7.29 리디자인 이후 계속 깨져 있었음** — 셀렉터가 구 `.tabs`
  ⑦ 고객 게시물 note와 채널 덱 리포트 문구의 문자열 규칙 위반 21곳 (생성 스크립트도 함께)
- **모바일 레이아웃 수리 ('26.7.30 사용자 지적)**: 실기기(아이폰)에서만 재현되던 것들 —
  헤드리스 크로뮴은 date·time 입력의 최소 폭이 작아 넘침이 안 잡혔다. ① **검색 필드 곡률 0**
  (사용자 표현 "곡률 없는 흰 바탕") — 곡률 통일 블록의 "검색은 밑줄" 규칙(`.search{border-radius:0}`)이
  뒤에 있어 v2 모바일의 라운드 글래스 필드를 눌렀다. 모바일에서만 18px 복원 ② **`.controls`가
  v1 흰 사각형** (background:var(--paper) + 잉크 밑줄) → 카드와 같은 유리 패널(반투명·blur·22px)
  ③ **모바일 상단 탭 바가 곡률 없는 흰 판** → 유리 표면 + 아래쪽 22px 곡률 ④ **RMN 캠페인
  기간 두 칸이 우측으로 밀림** — `.rmn-campperiod{align-items:flex-end}`가 세로로 쌓일 때
  각 칸을 내용 너비로 줄이고 오른쪽에 붙였다. 모바일에서 stretch로 복원 ⑤ **date·time 입력이
  카드를 뚫고 값이 가운데 정렬** — iOS는 고유 최소 폭을 갖고 값을 센터링한다.
  `-webkit-appearance:none` + `text-align:left` + `min-width:0` 전역 적용(네이티브 피커는 그대로)
  ⑥ **셀렉트가 iOS 기본 파란 글씨** — `.adm-taform`에 input·textarea만 스타일이 있고 select가
  빠져 있었다. **주의: select 보정에 `background` 축약형을 쓰면 커스텀 셰브론 이미지가 지워지고,
  border-radius를 쓰면 곡률 통일 18px을 덮는다** (둘 다 background-color만 쓰고 곡률은 안 건드림)
- **채널 덱 유사 전체화면 ('26.7.30)**: iOS 사파리는 영상이 아닌 요소에 `requestFullscreen`을
  구현하지 않아 아이폰에서 전체화면 버튼이 아무 일도 안 했다. 메서드가 없거나 호출이 거부되면
  `.dk-stage.faux`(position:fixed·100dvh)로 화면을 덮고 body 스크롤을 잠근다. 닫기 버튼과 Esc로 해제
- **홈 화면 설치 (PWA, '26.7.30)**: `public/manifest.webmanifest` + `public/icon-{180,192,512}.png`
  (favicon.svg를 렌더해 생성) + index.html 메타. 폰 홈 화면 아이콘에서 주소창 없이 전체 화면
  실행, 앱 전환 화면에도 별도 앱으로 표시. iOS는 apple-touch-icon과 apple-mobile-web-app-*
  메타를 봐야 하므로 매니페스트와 함께 유지. **서비스 워커는 두지 않는다** — 캘린더는 실시간
  데이터라 캐시가 낡은 일정을 보여줄 위험이 크다 (그래서 Chrome 설치 프롬프트 대신 메뉴의
  "홈 화면에 추가"를 쓰게 된다. 팀 대부분이 아이폰이고 iOS는 공유 메뉴 경로라 무영향).
  maskable 아이콘 미선언 — 심볼 여백이 18.75%라 안드로이드 원형 마스크에서 모서리가 잘림.
  iOS 홈 화면 앱은 사파리와 저장소가 분리돼 설치 후 한 번 다시 로그인해야 함(정상)
- 매체 아이콘: `src/ChannelIcon.jsx` — 실제 브랜드 컬러 칩(인스타 #E1306C, 유튜브 #FF0000,
  카카오 #FEE500+검정 글리프 등, channels.js color/fg 필드) + 라인 글리프 (이모지 금지 원칙)
- 등록 전 확인 팝업(ConfirmSheet): 매체 미인식 시 8종 선택 그리드 강제, 유사 캠페인 존재 시
  기존/신규 선택. 빠른 입력 아래 상태 칩 — 인식(실선)·미인식(빨간 점선) 구분
- 캠페인 이름 분산 방지: ① 입력 시 유사 캠페인명 제안 칩(클릭으로 통일), "#"만 치면 기존 캠페인
  노출 ② 캠페인 뷰 "이름 변경·통합" — 인라인 입력 폼 (기존 이름 입력 시 병합,
  store.renameCampaign). 네이티브 prompt/confirm 미사용 (환경별 차단 이슈) — 삭제도 2단계 버튼 확인.
  **제안 목록 2개월 필터 ('26.7)**: 마지막 게시 후 2개월 지난 캠페인은 제안(칩·datalist)에서
  제외, 최근 게시순 정렬 (CalendarPage campaigns memo) — 캠페인 뷰 보관 목록에는 계속 보임
- 기간 일정 표기: 시작일에 본 표기, 종료일에만 흐린(45%) "· 종료" 마커. 기간 중간 날짜는 표기 없음
- **하루 일정 시트 ('26.7)**: 셀이 넘칠 때(타겟APP 다건) 그날 전체를 큰 화면으로 —
  진입 = 일자 숫자 클릭 · "+N 더보기" 클릭 · (읽기 전용 뷰) 셀 클릭. 등록과 분리:
  빈 셀 클릭 = 등록(불변), 시트 하단 "+ 이 날짜에 등록" 버튼만 등록 진입 (읽기 전용은 숨김).
  행 클릭 = 개별 상세 모달, 기간 일정은 시작/종료/진행중 태그 (CalendarPage DaySheet)
- 역순 기간 방지 ('26.7): 수정 폼의 시작·종료 날짜 선택기를 거꾸로 고르면(종료<시작)
  저장 시 자동 정렬 (CalendarPage orderRange — onCreate/onSave 가드). 표시(fmtRange·
  indexByDay)에도 적용해 가드 이전 저장분도 화면에선 바로잡혀 보임
- 주의: Supabase 연동 전에는 localStorage(브라우저별 분리) — 서로 다른 브라우저에서는 일정·캠페인이
  안 보이는 게 정상. 기능 미동작으로 오인하기 쉬움
- **로그인 화면 가운데 배치 ('26.7.31 사용자 지시)**: 본문 폭(.wrap) 안에 얹혀 있어 빈 화면에
  카드만 좌상단에 떠 있었다. `.login-screen`(min-height 100dvh + flex 가운데) +
  `.login-inner`(로고, 제목, 설명, 카드를 한 덩어리로 중앙 정렬). 카드 안쪽만 좌측 정렬(입력 폼).
  모바일(≤560px)은 상단 14vh 기준 — 키보드가 올라와도 카드가 밀려 사라지지 않게
- 로그인 게이트 ('26.7): `src/lib/auth.js` — Supabase Auth REST 직접 호출(email/password),
  세션은 localStorage + 자동 갱신(만료 1분 전 refresh_token 사용). REMOTE 모드에서는
  미러(?view=mirror) 포함 캘린더 전체가 로그인 필수 (URL 조작으로 내부 일정 접근 불가).
  쓰기 권한은 RLS + `team_writers` 테이블(이메일 등록제, security definer 함수 `is_team_writer()`)로
  분리 — 뷰어 계정(미등록)은 자동 읽기 전용. 절차는 supabase-setup.md 4장. 계정은 자율 가입 금지,
  Authentication → Users에서 직접 발급. 매체 스펙 탭·로컬 테스트 모드는 로그인 불필요
- 날짜 셀 클릭 → 신규 등록 모달 (EventModal isNew 모드, 시작일 프리필). 읽기 전용 뷰에서는 비활성.
  **모바일 컴팩트 등록 ('26.7)**: ≤560px에서 신규 모달 = **상단 시트**(화면 ~27%) — 한 줄 자동
  작성 + 인식 요약 칩 + "상세 입력 펼치기" 버튼만, 긴 폼은 펼쳐야 노출 (EventModal expanded).
  하단 시트로 했다가 자동 포커스 키보드가 등록 버튼을 덮어 상단으로 변경 ('26.7 2차).
  모바일 최적화 일괄: html/body overflow-x hidden(좌우 밀림 차단), 모바일 전 입력 16px
  !important(iOS 포커스 확대 = 좌우 이동 원인 차단), placeholder는 12.5px + 모바일 전용
  짧은 예시(isMobile), viewport meta interactive-widget=resizes-content, 모달 overflow-x
  hidden, qa-btn nowrap. 등록 버튼은 제목·날짜 없으면 disabled. 데스크톱 모달은 불변.
  **월간 그리드 모바일 가독성 ('26.7 3차)**: 셀 min-height 88px·일자 13px, 일정 제목
  2줄 클램프(클램프는 .cal-ev 버튼에, 칩은 인라인 흐름 — 좁은 셀에서 제목이 전체 폭 사용)
- **드래그앤드롭 이동 ('26.7)**: 월간 그리드에서 일정을 끌어 다른 날짜로 이동 — 세 캘린더 탭
  (매체·팀·촬영) 공통. 6px 이동 임계값(그 전에 놓으면 기존 클릭=모달, 셀 클릭=등록 불변),
  기간 일정은 어느 날짜(시작·"· 종료" 마커)를 끌어도 기간 유지 평행이동, 이동 직후 8초
  "실행 취소" 바(하단 고정, MonthGrid pickUp + CalendarApp onMove/onUndo). 터치(모바일)는
  스크롤 충돌로 제외 — 데스크톱 전용, 모바일은 기존 수정 폼. 기념일 투영(orig)·읽기 전용
  뷰는 드래그 불가. 이동도 updateEvent라 변경 이력·알림센터에 자동 기록
- 전체 일정 검색 ('26.7): 캘린더 상단 검색창 — 제목·캠페인·메모·작성자·매체 색인, 검색 중엔
  월간/캠페인 뷰 대신 결과 리스트(최신순·하이라이트·클릭 시 모달)
- 캘린더→스펙 딥링크 ('26.7): 일정 모달 안 "이 매체 규격·납기 보기" 텍스트 링크(삭제·수정과 분리,
  미스클릭 방지 설계 — 셀 클릭 동작 불변). 매핑은 `src/lib/specLink.js`, 매핑 없으면 링크 숨김.
  스펙 탭 진입 시 해당 매체 자동 펼침+중앙 스크롤+배경 강조 (App.jsx specFocus)
- 메모 = 설명+참고 링크 ('26.7): 상세 모달에서 http/https 자동 링크(새 탭). 메모 있을 때만 줄 노출
- **일정 이미지 첨부 ('26.7 — 결과·시안 보고용)**: 상세 모달 "＋ 이미지 첨부" — 업로드 시
  브라우저 자동 압축(settleStore.compressImage 재사용, 긴 변 1600px JPEG — 3MB 폰 사진 →
  ~300KB), 일정당 최대 5장·파일당 10MB. 썸네일 클릭 = 라이트박스(ref-lightbox 재사용),
  삭제 = 2단계 확인. 실파일은 **event-images 공개 버킷**(캘린더를 매체 보고용으로 쓰는
  목적상 미러에서도 보여야 — 일정의 미러 anon SELECT와 같은 공개 수준, 경로에 UUID·
  타임스탬프라 링크 소지자만. 업로드·삭제는 로그인 계정 전체('26.7.27 완화 — team_writers 한정이 팀원을 막던 문제,
  메타 저장은 여전히 team_writers RLS라 뷰어는 최종 차단), 메타는 `media_events.images`
  jsonb (**사용자 액션: setup.md 10장 SQL 1회** — 미실행 시 첨부 저장만 실패, 무영향).
  store.js `updateEventImages` = images만 부분 PATCH — toDb의 images는 **조건부 전송**이라
  일반 수정·드래그 이동이 첨부를 안 덮음 (test-data 9 회귀 감시). 변경 이력에는 "이미지:
  N장 → M장"으로 표기. 일정 삭제 시 실파일 보존(어드민 삭제 복원이 이미지까지 살리게) —
  개별 × 삭제만 실파일 제거. 읽기 전용 뷰(미러 포함)는 열람만. src/lib/eventImages.js.
  **공용 위젯 + 붙여넣기 ('26.7)**: 그리드·라이트박스·2단계 삭제·**Ctrl+V 붙여넣기 업로드·
  드래그앤드롭 업로드('26.7.27)**는
  `src/ImageAttach.jsx` 공용 컴포넌트 — 캘린더 일정·RMN 부킹이 같이 씀.
  **라이트박스는 createPortal로 body에 렌더 ('26.7.29 수리)** — 모달의 backdrop-filter가
  position:fixed의 containing block이 되어 세로 이미지가 모달 안에 갇혀 스크롤해야 보였음.
  이미지도 max-height 92vh + object-fit contain으로 한 화면에 들어오게 고정.
  **여러 장이면 좌우 넘기기 ('26.7.29)** — 화살표 버튼·키보드 ←→·모바일 스와이프(40px)·
  하단 "N / M" 카운터, 끝에서 순환. Esc로 닫힘, 한 장이면 화살표 미표시.
  삭제로 장수가 줄면 인덱스를 범위 안으로 당김. 오버레이는 어둡게(.86)+blur로 이미지 집중 붙여넣기는 위젯이
  마운트된 동안만 동작이라 호출측은 한 번에 하나만 열 것 (RMN은 패널 단일 열림으로 보장).
  **설치 오진 사고 ('26.7.24)**: setup 10장을 한 번에 Run 하면 SQL Editor가 통째로 한
  트랜잭션이라 뒤쪽 storage 정책문이 실패할 때 **앞의 버킷 생성까지 롤백** — "성공" 후에도
  버킷이 없어 업로드가 막혔다. → setup.md 10장을 ①컬럼 ②버킷 ③정책 3회 실행으로 분리 +
  설치 확인 쿼리 추가. eventImages.req도 상태코드 뭉뚱그리기를 걷어내고 버킷없음/권한/기타를
  구분하며 서버 원문(detail)을 노출. 업로드는 x-upsert 제거(경로에 타임스탬프라 충돌 없고,
  upsert는 UPDATE 정책까지 요구해 RLS에 걸릴 수 있음).
  **한글 파일명 사고 ('26.7.27)**: Storage 키는 ASCII만 허용 — "아파트.jpg" 업로드가
  Invalid key 400. 경로를 타임스탬프+난수+확장자로 변경(원본 이름은 메타 name에만 보존,
  ZIP·다운로드는 메타 이름 사용이라 무영향). eventImages·settleStore 동일 적용
- 작성자 자동 기록 ('26.7): 로그인 계정 → `src/data/team.js`(이메일→이름+직급 매핑)로 자동 기록.
  수동 입력칸 제거, 수정 폼에서만 교정 가능. 명단 미등록 계정은 이메일 앞부분 폴백.
  **팀원 명단 등록 완료 ('26.7 — 20명, team.js).** 인원 변동 시 team.js 한 줄 추가·수정
- 다중 매체 한 줄 등록 ('26.7): "인스타+유튜브+카톡" → 매체 수만큼 동시 등록 (parse.js
  extractChannels — 모든 +조각이 매체로 인식될 때만, "1+1 행사"는 오인 안 함). 제목에서
  +그룹은 제거, 상태 칩에 매체별 칩 + "N건 동시 등록" 표시. 세부끼리도 동작
  ("바이비+아파트너+카카오골프" → 타겟APP 3건)
- **세부 다중 선택 등록 ('26.7)**: 신규 등록 모달(날짜 클릭)에서 세부가 있는 매체를 고르면
  단일 select 대신 **체크박스 그리드**(EventModal multiSub, 팀 일정 제외) — 체크 수만큼 동시
  등록, 3개 이상 세부면 "전체 선택" 버튼. 한 줄 자동 작성의 +세부도 체크에 반영.
  수정 모드는 단일 select 유지 (1건=1세부 모델 불변)
- **타겟APP 셀 묶음 ('26.7)**: 같은 제목·기간의 타겟APP 형제 건(세부만 다름)은 월간 셀에서
  "×N" 한 줄로 합침 (MonthGrid groupCellEvents — 10종 동시 등록 시 셀 도배 방지, 타겟APP만).
  클릭 = 세부 선택 시트(개별 상세로 진입), 드래그 = 형제 전체 평행이동(실행 취소도 전체).
  다른 매체 묶음은 미적용 ('26.7 사용자 결정).
  **타겟APP 후순위 ('26.7)**: 묶음 뒤에도 타겟APP이 한 날짜에 여러 캠페인이면 셀 상한
  (기본 4·와이드 8)을 혼자 채워 다른 매체가 "+N 더보기"에 묻혔다 → 셀·하루 시트 모두
  **타겟APP을 맨 뒤로** (CalendarPage targetLast + DaySheet chOrder 999). 안정 정렬이라
  나머지 매체 순서는 불변, 세 캘린더 탭·미러 공통
- 팀 일정 탭 ('26.7): 연차·반차·외근·출장·교육·업무·기념일 캘린더 (`#team`, CalendarPage team 모드) —
  같은 테이블 `kind='팀'`, 유형은 channel 필드에 저장 (channels.js TEAM_TYPES·TEAM_KEYWORDS,
  기타는 id 충돌 방지 위해 '팀기타'). 빠른 입력 동일 문법("7/20 김희진 연차"), 매체 표기
  통일·캠페인은 팀 모드 미적용 (parseQuick opts). **제목은 쓴 그대로 표기** ('26.7 확정) —
  "김상수, 정소미, 노규빈 목동 외근"처럼 대신 등록 시 이름들+유형이 캘린더 셀에 그대로 보임
  (유형 단어를 제목에서 제거하지 않음, TITLE_STRIP 미적용). **기념일(생일 등)은 1건 등록 =
  매년 자동 반복** (표시 시점 투영, 원본 불변). 미러·매체 캘린더에는 미표시.
  SQL 불필요 (kind 컬럼 재사용). **이름 자동 병기 ('26.7)**: "7/16 연차"처럼 이름 없이 쓰면
  등록 시 작성자 이름(직급 제외)을 제목 앞에 붙임 → "노규빈 연차". 명단(team.js) 이름이
  제목에 하나라도 있으면(대신 등록) 그대로 둠 — team.js withAuthorName, 등록 시점 적용(소급 없음),
  test-data.mjs 6b 케이스
- **내 일정 탭 ('26.8.7 — 시안 승인 후 실구축)**: `#mytask`, `src/MyTaskPage.jsx`(React.lazy) —
  좌측 개인 투두 패널 + 우측 개인 캘린더(월간, 주간 시간 격자). 투두를 캘린더로 끌어다 놓으면
  일정이 되고, 패널로 다시 끌면 날짜 지정이 없어진다. **config.js `MYTASK_EMAILS` 게이트**
  (1차는 노규빈만, 팀 오픈은 이메일 한 줄 추가나 App.jsx `isMyTask` 게이트 제거).
  **사용자 액션: data/mytask-setup.sql 1회 (setup.md 15장)** — 미실행 시 안내 문구만
  - **개인 데이터는 media_events에 절대 넣지 않는다** — 미러가 열리면 그 테이블에
    무로그인 읽기 정책이 테이블 전체로 걸리므로 개인 메모가 샌다. 그래서 `my_spaces`,
    `my_todos`, `my_events` 세 테이블로 물리 분리. **이 저장소 첫 owner-or-shared RLS**
    (읽기 = 본인이거나 shared_with에 내 이메일 / 수정 = 본인이거나 shared_edit 켜진 공유
    대상 / 생성·삭제 = 본인만). 기존 테이블은 전부 "로그인하면 다 보임"이라 패턴이 다름
  - 팀 캘린더 연동은 상세 시트 체크박스 — 켜면 media_events에 `kind='팀'`으로 한 건 더
    만들고 id를 `linkedId`에 물려둔다(라운지 linked_events와 같은 패턴). 끄거나 날짜를
    없애면 그 행만 지운다. **팀에 넘어가는 것은 제목과 날짜뿐** (메모·공유 설정 미전달)
  - 팀·매체·촬영 일정은 **읽기 전용 배경 칩**으로만 얹고(겹쳐보기 레이어 토글), 내 일정과
    시간까지 겹치면 클레이 테두리 + 하단에 겹침 요약. 스페이스는 각자 자유 생성
    (인라인 입력·2단계 삭제 — 네이티브 prompt·confirm은 환경별 차단 이력으로 미사용)
  - **드래그 함정 3개는 시안에서 실측해 잡았다** ① 드래그 끝에 브라우저가 click을 한 번 더
    쏜다 → `suppress` 플래그로 억제하지 않으면 상세가 저절로 열림 ② 드래그가 텍스트 선택을
    남기면 다음 드래그를 네이티브 드래그로 오인해 **pointercancel로 끊긴다** → 칩에
    `user-select:none` 상시 적용 + pointerdown `preventDefault` ③ 같은 시간대 일정은 폭을
    나눠야 한다(겹쳐 쌓으면 아래 것이 클릭조차 안 됨). **같은 취약점이 기존 매체 캘린더
    MonthGrid 드래그에도 있어 함께 수리** ('26.8.7 — `.cal-grid.dragging`에만 걸려 있던
    user-select 해제를 `.cal-grid.editable .cal-ev` 상시로, pointercancel 정리 리스너 추가.
    없으면 리스너가 남고 그리드가 드래그 상태로 굳는다)
  - 길이 조절은 주간 칩 하단 그립을 끌어 30분 단위 스냅. 시간 없는 일정은 주간 맨 위
    "종일" 줄. 드롭 타깃은 `elementFromPoint`로 찾은 `[data-date]`라 드래그 소스와 무관
  - **할 일 상세와 메모·이미지 ('26.8.7 사용자 지시 3건)**: 할 일 행 클릭 = 상세 시트
    (TodoSheet — 제목 인라인 수정, 메모, 이미지, 스페이스, 공유). 드래그와 클릭이 같은
    타깃이라 DragLayer의 `suppress`가 그대로 쓰이고, 체크박스·× 는 클릭 경로에서 제외.
    메모는 포커스를 떠날 때만 저장(MemoBox — 타이핑마다 PATCH를 쏘면 요청이 폭주).
    **메모와 첨부는 할 일↔일정 변환에서 그대로 따라간다** (dropTodo·unschedule 양방향)
  - **첨부는 비공개 버킷 `mytask-img`** — 일정 첨부(event-images)는 미러 표시가 목적이라
    공개지만 개인 할 일 첨부는 링크만 알면 열려도 안 된다. 경로 첫 칸이 소유자 폴더고
    Storage 정책이 남의 폴더를 막는다. **폴더 규칙(이메일의 @ 와 . 을 _ 로)은 SQL 정책과
    `src/lib/myTaskImages.js` folderOf가 글자 그대로 같아야 함** (다르면 업로드가 403,
    test-data 17이 일치를 감시). 비공개라 `<img src>`로 못 읽으므로 토큰 fetch 후 blob URL.
    이를 위해 **`ImageAttach`에 저장소 어댑터 `api` prop 추가** (기본값 = 공개 event-images라
    일정·RMN 첨부는 불변, `api.resolve`가 있으면 blob URL을 캐시하고 언마운트 때 revoke)
  - **스페이스 지정 3갈래** ① 입력줄 아래 "담을 곳" 칩 ② 스페이스 필터가 켜져 있으면 그쪽
    ③ 본문에 `#기획`이라고 쓰면 그 스페이스로, 없으면 즉석 생성 (캠페인 #문법과 같은 결.
    태그는 제목에서 제거)
  - **스페이스별 정렬 ('26.8.7 사용자 지시)**: 할 일 목록 헤더에 [스페이스별 | 최근순] 세그,
    기본은 스페이스별. 스페이스마다 접히는 그룹 머리(색점, 이름, 남은 건수)로 묶고
    **분류 없음은 항상 맨 뒤** (정리 안 된 항목이 위로 오면 목록이 어수선해진다).
    완료 항목은 어느 정렬에서도 그룹 안 아래쪽. 정렬 모드와 접은 그룹은 localStorage
    (`mtTodoSort`, `mtTodoFold`) 기억. 그룹 머리가 이미 스페이스를 보여주므로 행의
    스페이스 칩은 최근순일 때만 표시(TodoRow `showSpace`)
  - 회귀 감시 = `npm run audit`에 `#mytask` 추가 (my_* 목킹 시드 포함).
    **config.js에 export를 추가하면 scripts/smoke.mjs의 스텁에도 한 줄 추가할 것** —
    빠지면 빌드가 깨진다
  - **전면 개선 ('26.8.8 사용자 지시 "ARCH Calendar 참고, 레이아웃 전반도 편의성 개선")**.
    벤치마크 결론 = 구조는 ARCH와 이미 같고(인박스 + 스페이스 + 드래그 타임블로킹),
    스페이스 정렬과 팀 일정 겹쳐보기는 오히려 앞선다. 모자란 건 기능 개수가 아니라
    **일정을 만드는 진입 경로**였다 (드래그 하나뿐이라 폰에서는 0개)
    - **판정 로직을 `src/lib/myTask.js`로 분리** — MyTaskPage.jsx는 JSX라 node가 못 읽어
      회귀 감시가 안 됐다. overlaps·findClashes·layoutDay·expandBg·hoursFor·parseLine을
      옮기고 **test-data 20번**이 직접 호출해 검사
    - **겹침 판정이 반대로 동작하고 있었다** — 배경 일정(팀·매체·촬영)은 구조상 시각이
      없는데 "한쪽이라도 시각이 없으면 겹침"이라, 매체 일정이 있는 날이면 내 일정 전부에
      빨간 경고가 붙었다(매체 캘린더는 거의 매일 일정이 있다). 반대로 **내 일정끼리의
      이중 예약은 조용히 지나갔다**(상대가 내 일정이 아닐 것이라는 조건). 둘 다 뒤집음
    - **주간 격자 좌표계 재작업** — 칩을 시각 칸이 아니라 **요일 열 전체**에 절대 배치.
      예전엔 1시간을 넘는 칩이 아래 칸(DOM 순서상 뒤라 위에 그려짐)에 덮여 **길이 조절
      그립을 잡을 수 없었고**, 폭 분할이 "같은 시(hour)에 시작한 것"끼리라 09:00~11:00과
      10:00은 둘 다 전체 폭이라 아래 것이 클릭조차 안 됐다. 덤으로 한 줄이 정확히 8칸이
      되어 `nth-child(9n)`이 격자 한가운데에 대각선으로 세로선을 빼먹던 것도 사라짐.
      시각 칸은 이제 칩 뒤에 깔린 **30분 단위 드롭 타깃 겸 빈 칸 등록 버튼**
    - **시간 범위 동적** — 8~20시 고정이라 07:30이나 21:00 일정이 종일 줄에도 시각 줄에도
      안 걸려 주간 화면에서 통째로 사라졌다. 분 단위 위치도 이제 반영(10:30이 10:00과
      같은 자리에 그려지던 것)
    - **일정을 만드는 길 셋** ① **빈 칸 클릭** (월간 셀 ＋ 버튼, 주간 30분 칸 — 시각까지
      프리필) ② **한 줄에 날짜** ("내일 15시 임원 보고 1시간" — parse.js 파서 재사용,
      입력 아래에 해석 결과를 미리 보여줌. **parseQuick에 `labels:false` 옵션 추가** —
      촬영·업로드 라벨 해석이 "촬영 준비"의 촬영을 먹어 제목에서 지웠다. 기본값은 true라
      매체·촬영 탭 불변) ③ **드래그** (데스크톱 즉시, 폰은 350ms 롱프레스)
    - **폰에서는 드래그가 성립하지 않는다** — ≤1000px에서 패널과 캘린더가 세로로 쌓여
      한 화면에 같이 안 보인다. 그래서 **할 일 상세에 "날짜 지정"**(날짜 + 시각 + 일정으로
      버튼)을 두는 것이 폰의 주 경로고, 롱프레스 드래그는 캘린더 안 이동용
    - **일정 상세에 날짜와 제목 입력 추가** — 예전엔 드래그가 유일한 이동 수단이고 제목은
      "날짜 해제 → 할 일에서 수정 → 다시 드래그" 3단계였다
    - **실행 취소 바 8초** (이동·날짜 해제·삭제) — `.mt-todos` 전체가 드롭존이라 왼쪽
      아무 데나 놓기만 해도 날짜가 풀리고 id가 바뀌어 되돌릴 방법이 없었다
    - **낙관적 갱신** — 체크 한 번에도 서버 왕복을 기다렸다. 실패 시 직전 상태로 롤백
    - **공유받은 항목이 목록에서 통째로 사라지던 것** — 남의 스페이스에 든 항목은
      내 spaces 어디에도 안 걸려 기본 정렬(스페이스별)에서 빠졌다. **"공유받음" 그룹**
      추가 + 소유자 배지, 남의 것은 드래그·삭제 불가(누르면 권한 오류로 끝났다)
    - **기간 배경 일정을 걸치는 날마다** 표시 (8/1~8/20 매체 집행이 8/1에만 떠서
      "겹치는지 확인"이라는 목적 자체가 성립하지 않았다). 보고 있는 기간 밖은 잘라냄
    - 그 밖 — 월간 셀 상한 4건 + "N건 더 보기", 할 일 목록 자체 스크롤(완료가 쌓이면
      패널이 수천 px), **오늘 요약 블록**(내 일정 수·배치된 시간·팀 일정 수), 검색,
      완료 숨기기, 크게 보기(패널 접기), **단축키 N / · T M W ← → ?**(입력 중 미발동),
      지금 시각 선, 드래그 중 따라오는 칩에 놓일 시각 표기, 휴점 DB 병합,
      뷰·레이어 localStorage 기억, 주간 제목을 범위로(8/31~9/6이 "2026.08 31일 주"로
      보였다), 스페이스 삭제 시 "담을 곳" 해제(FK 409), 0행 응답을 한글 문구로
      (PostgREST는 RLS에 걸리면 403이 아니라 200 + `[]`를 준다 — 영문 TypeError가 떴다),
      Esc로 닫아도 메모 저장(언마운트 시 저장 — 포커스된 노드가 언마운트되면 onBlur가
      오지 않는다), 실패한 길이 조절의 인라인 height 복원, 변환 중간 실패 시 롤백,
      팀 연동 일정에 작성자 기록, 접근성(체크박스 role·focus-visible·title)
    - **회귀 감시 = `npm run audit:mytask`** (`scripts/audit-mytask.mjs`) — 위 21개를
      실제 브라우저에서 눌러 확인. 데스크톱 1440px + 폰 390px 터치까지
    - **아직 안 한 것**: 할 일 순서 직접 바꾸기(`sort` 컬럼은 있으나 UI 없음), 반복 일정,
      개인 일정 삭제 이력(my_* 에는 트리거가 없어 복원 불가)
  - **레이아웃 2차 ('26.8.8 사용자 지시 — ARCH Calendar 화면 첨부)**: 참고 이미지의
    3단 구성을 따라감
    - **가장 좌측 분류 레일** (`SpaceRail`, `.mt-rail`) — 접으면 56px 색점만, 펼치면
      170px에 이름과 남은 건수. 접기 상태는 localStorage `mtRail`. 스페이스 추가와
      삭제도 여기서 한다. **≤1000px에서는 가로 스크롤 띠로 전환** (세로 열로 두면
      폰 화면을 통째로 먹는다). 할 일 패널 안에 있던 분류 칩 줄은 제거
    - **할 일 입력 한 줄로** — 추가 버튼을 없애고 엔터로만, 담을 곳은 입력줄 왼쪽
      **색점을 눌러 고르는 팝오버**로. 분류 칩 줄과 담을 곳 칩 줄 두 개가 입력 영역을
      아래로 밀어내던 것을 해소
    - **주간 칩을 분류 색 반투명으로** (`myTask.js tint`) — 배경은 색 15%, 왼쪽에 3px
      색선, 글자는 잉크. 예전엔 내 일정이 전부 같은 그린이라 칩만 보고는 분류를 알 수
      없었다. 배경 일정(팀·매체·촬영)은 8%로 더 옅게. **칩마다 시각 표기**
      (`timeLabel` — "10:00 - 11:30 / 1시간 30분", 30분짜리처럼 두 줄이 안 들어가는
      높이에서는 제목만 남기고 시각은 title 속성으로)
    - **상세 시트의 날짜 칸이 깨져 보이던 것** — `.mt-sh-row`가 `input[type=time]`과
      `select`만 스타일하고 **`input[type=date]`가 빠져 있어** 그 칸만 네이티브 기본형
      으로 튀었다. 곡률 18px·유리 표면·높이를 시각 칸과 맞추고, iOS의 고유 최소 폭과
      가운데 정렬도 함께 눌렀다(`-webkit-appearance:none` + `text-align:left`)
    - **격자가 열 밖으로 삐져나가던 것** — 트랙이 `1fr`이라 종일 줄의 nowrap 칩
      min-content가 트랙을 밀어냈다. `minmax(0,1fr)`로 잠그고 `.mt-gwrap`에
      `overflow-x:auto` 안전망. **주의: 그리드 항목은 min-width가 auto라, 안쪽에
      nowrap 요소가 있으면 트랙이 그만큼 벌어진다** (회의록 카드도 같은 원인이었다)
    - **`0`이 칩마다 찍히던 것** — `{(e.memo || e.images?.length) && …}` 에서
      `images`가 `[]`면 `[].length`가 `0`이고 **React는 0을 문자로 그린다**.
      `!!(…)`로 감싸야 한다 (`&&` 단축 평가로 조건부 렌더할 때 상시 주의)
    - 주간 칩 제목은 두 줄까지 (`-webkit-line-clamp:2`) — 한 줄 말줄임이면 좁은 열에서
      "대행..."만 남는다. 종일 줄에서 "종일"이라고 또 쓰던 중복 표기 삭제
- **회의록 탭 ('26.8.7 — 사용자 지시 "우리 사이트에 녹음 버튼이 붙고, 끝나면 요약")**:
  `#minutes`, `src/MinutesPage.jsx`(React.lazy) — 사이트 안에서 바로 녹음하고 받아쓰고 요약.
  **config.js `MINUTES_EMAILS` 게이트** (1차는 노규빈만).
  **사용자 액션: data/minutes-setup.sql 1회 (setup.md 16장)** + Vercel `ANTHROPIC_API_KEY`
  (월간 리포트에 이미 있으면 추가 작업 없음)
  - **Claude는 음성을 못 읽는다** — 전사는 별도 단계다. 그런데 **크롬 내장
    SpeechRecognition**을 쓰면 계정도 키도 계약도 없이 한국어 실시간 전사가 된다
    (`src/lib/recorder.js`). MediaRecorder(음성 파일)와 동시에 돌린다.
    STT 비용 0원, 요약만 Claude(1시간 회의 약 300원)
  - **브라우저 제약 3개는 화면에 그대로 적는다** (숨기면 회의가 통째로 날아간다)
    ① 받아쓰기는 크롬 계열 전용, 사파리는 녹음만 됨 ② 탭이 백그라운드로 가거나 화면이
    잠기면 마이크와 타이머가 죽음 ③ 크롬 인식은 음성을 구글 서버로 보냄.
    test-data 18이 이 세 문구의 존재를 회귀 감시
  - **음성 인식은 조용하면 스스로 멈춘다** — `onend`에서 다시 켜지 않으면 회의 중간부터
    전사가 통째로 빈다. 사용자가 멈춘 것과 구분하려고 `stopped` ref를 둔다
  - **종료 시 전사문은 ref에서 읽는다** (`textRef`) — 화면 상태(`r.text`)를 읽으면
    마지막 문장이 빠진다. `stop()`이 `{blob, text, sec}`을 함께 돌려준다
  - **종료·일시정지 버튼 배치 사고 ('26.8.7)**: 종료(왼쪽 끝)와 일시정지/이어서 녹음
    (오른쪽 끝) 사이를 flex:1 레벨미터가 채워 화면 폭에 따라 1000px 넘게 떨어져 있었다.
    일시정지를 누르면 시선이 오른쪽에 머물러 종료 버튼을 놓치기 쉬운 배치 —
    `.mn-rec-btns`로 두 버튼을 붙이고 시간·레벨미터를 뒤로 옮김
  - **stop 이벤트 안전장치 ('26.8.7 사용자 리포트로 실측)**: 일시정지 후 곧바로
    종료하면 브라우저에 따라 MediaRecorder가 `stop` 이벤트를 영영 안 쏘는 경우가
    드물게 있다. 그러면 `finish()`가 `stop()`의 Promise를 못 받아 "저장 중"에
    무한정 멈추고, 화면엔 일시정지 상태의 "이어서 녹음" 버튼만 멀쩡해 보여
    "종료가 안 된다"로 오인된다. **4초 안에 `stop` 이벤트가 없으면 지금까지 모은
    조각으로 강제 완료**(`finalize` + `setTimeout`). `pause()`도 예외를 던지는
    구현체를 대비해 try/catch로 감싸 화면 상태 전이가 항상 진행되게 함.
    실제 브라우저 고장을 흉내 낸 QA로 재현·검증(발화 있으면 4.3초 만에 정상 저장)
  - 요약 = `api/minutes-summarize.js` (report-narrate와 같은 패턴, claude-opus-5,
    구조화 출력으로 개요·결정사항·액션아이템·논의·확인필요). **녹취록에 없는 결정,
    담당자, 날짜를 만들지 않는다**가 시스템 프롬프트 1번 규칙, 불확실하면 빈 값
  - **액션아이템 → 내 할 일** (myTaskStore.createTodo 재사용, 스페이스 지정 가능).
    담은 것은 `todo_ids`에 인덱스를 남겨 중복 등록을 막는다
  - 음성은 **비공개 버킷 `minutes-audio`**, 소유자 폴더 규칙은 mytask-img와 같은 방식.
    RLS도 my_* 와 같은 owner-or-shared (회의 내용은 개인 데이터급)
  - 붙여넣기 경로도 있다 — 아이폰 음성 메모 대본이나 팀즈 전사문을 넣으면 요약은 동일.
    사파리 사용자와 대면 회의 녹음의 폴백
- **캠페인 태그 없는 일정 자동 묶음 ('26.7.30 사용자 지시)**: 홈 "주요 콘텐츠"가 `#태그`
  붙은 일정만 보여줘서 웨딩페어나 미아 25주년처럼 명백히 한 주제인 건들이 흩어져 있었다.
  `src/lib/autoGroup.js` — 제목에서 공통으로 나오는 말을 찾아 2건 이상이면 한 묶음.
  **채널·포맷·행사 상투어는 STOP으로 제외** (인스타·릴스·팝업·오픈·행사 등, 이걸로 묶으면
  무관한 일정이 한 덩어리가 된다). 후보는 토큰 1~3개 구절이고 **순위 = 걸치는 건수 ×
  구절 길이** — 건수만 보면 "미아 25주년"과 "천호 25주년"이 "25주년"으로 뭉개지고,
  길이만 보면 "웨딩페어"가 "웨딩페어 사전예약"에 쪼개진다. **사람이 붙인 태그가 항상 우선**
  (태그 없는 일정만 넘김). 화면에서는 **점선 회색 칩(`.home-camp.auto`)** 으로 정식 태그(그린 #)와
  구분 — 추론이라 틀릴 수 있음을 생김새로 알린다. KPI "진행 중인 캠페인"은 **태그 기준 그대로**
  (클릭 시 가는 캠페인 세그가 태그만 보여주므로). test-data 12가 회귀 감시
- 홈 ('26.7): 접속 첫 화면 `src/HomePage.jsx` — 중요도순 ①오늘·내일 팀원 근태 ②주요 콘텐츠
  (캠페인 단위 D-day/진행중, 3주 내) ③이번 주 하이라이트(유튜브 썸네일 카드 — thumb 필드,
  '26.7 수집분부터 + 인스타 반응 상위). 섹션별 "전체 →" 링크로 탭 전환, 데이터 없으면 섹션
  숨김(근태만 "전원 근무" 상태 문구). 추후 섹션은 HomePage.jsx 하단 주석 자리에 추가.
  **기념일 축하 팝업** `src/Celebration.jsx` — **글래스모피즘 전환 ('26.7.30 사용자 지시
  "지금과 어울리게, 일러스트도 심플하되 축하는 충분히")**: 원색 카드 + 검정 실루엣이
  v2 화면에서 혼자 튀어 사이트 언어로 되돌림. 카드는 유리 표면(--glass-strong + blur 26px,
  곡률 24px) + 안쪽 그린 글로우, 오버레이는 그린 잉크 + blur 8px.
  축하하는 느낌은 색 면적이 아니라 ① **컨페티** (`CONFETTI` 고정 좌표 14조각, 브랜드
  팔레트 + 형광 2조각, 렌더마다 난수를 쓰면 위치가 튄다) ② **그린 선화 글리프**
  (검정 덩어리 → 가는 획, ChannelIcon 라인 글리프와 같은 결. 생일 = 촛불 1개 + 광선 +
  프로스팅 물결 케이크, 결혼 = 겹친 두 링 + 반짝임. **초를 여럿 그리면 나이로 읽힌다**)
  ③ 큰 영문 워드마크(그린)로 낸다. 푸터는 영문 대문자 자간 확대(eyebrow 패턴) 대신
  "미디어콘텐츠팀". **홈 탭에서만 렌더**(팀 일정 탭 아님).
  기념일이 주말·공휴일이면 **직전 영업일에 미리** 띄움(일→금, 연휴 연속 당김·holidays.js 기준).
  하루 1회 localStorage 스탬프. 구 TeamNotice.jsx(근태 팝업)는 홈 근태 섹션으로 대체·삭제.
  히어로 지표 4개는 클릭 시 해당 탭 이동(게시 예정·캠페인→매체 캘린더, 촬영→촬영 캘린더, 부재→팀 일정).
  부재 지표는 "오늘 팀원 부재" ('26.7 변경 — 오늘이 기간에 포함되는 팀 일정만, 기념일 제외).
  섹션 순서: ①근태 ①-b**주요 업무·마감**('26.7 — 팀 일정 "업무" 유형만, D-day 표기, 3주 내
  최대 6건) (~~RMN 영업 현황 카드~~ — '26.7.28 사용자 지시로 홈에서 제거, RMN 탭에서만)
  ②주요 콘텐츠 D-day ③이번 주 촬영 ④**채널 이슈**(구 "채널 시그널",
  '26.7 개명 — 모니터링 하이라이트 중 계정 신호만 재사용) ⑤이번 주 하이라이트. 팀 사용 가이드는 docs/team-guide.md
- **업무 일정 유형 ('26.7)**: TEAM_TYPES에 '업무'(현대그린) — 키워드 업무·마감·회의·보고·제출
  (근태 키워드보다 뒤 = "출장 보고"는 출장 우선). 이름 자동 병기 제외(팀 공용 일정),
  홈 근태 섹션·부재 지표에서도 제외(전용 섹션에만). 예: "8/1 임원 보고 자료 마감"
- 촬영일정 탭 ('26.7): 유튜브·인스타 전용 별도 캘린더 (`#shoot`, CalendarPage shoot 모드) —
  일정은 같은 테이블의 `kind='촬영'`으로 구분 (매체 캘린더에는 미표시, 미러 사이트에도 없음).
  병기 문법: "7/10 촬영 7/15 업로드 제목 인스타" 한 줄 → 촬영 건(촬영탭) + 업로드 건(매체
  캘린더) 동시 등록. 라벨 표기: 촬영 / 업로드·발행·게시. 촬영탭에서 날짜만 쓰면 촬영일.
  다른 매체로 촬영 등록 시도 시 에러. **Supabase `kind` 컬럼 필요 (setup.md 5장 SQL 1줄) —
  미적용 시 촬영 등록만 실패, 기존 기능 무영향**
- 변경 이력 ('26.7): DB 트리거가 등록·수정·삭제 자동 기록(`media_events_history`,
  setup.md 6장 SQL) — 일정 모달 "변경 이력"(바뀐 필드 diff 표시) + 캘린더 하단 "최근 30일
  삭제 기록"(접힘, 촬영/일반 분리). REMOTE 모드 전용(로컬은 숨김), 읽기는 로그인 계정만.
  테이블 미설정 시 안내 문구만 뜨고 다른 기능 무영향
- 실적 확정 ('26.7): 실적 후보 목록에서 담당자가 "선택"을 누르면 그 콘텐츠만 확정으로 남음
  (`perf_url` 컬럼 — setup.md 5장 SQL, 미적용 시 확정 저장만 실패). "선택 해제"로 후보 복귀.
  읽기 전용 뷰는 확정분만 보고 버튼 없음. 일반 수정·저장은 확정값을 건드리지 않음
- 모니터링 하이라이트 ('26.7): 탭 상단 자동 브리핑 — 규칙 기반(비용 0): 팔로워 급증·급감
  (직전 수집 대비 1% 또는 50명↑), 새 휴면 진입, 유튜브 조회 급등(3주 내 게시 & 채널 평균 3배↑).
  직전 스냅샷 없으면 급증·휴면 항목은 생략, 표시할 게 없으면 섹션 숨김. 최대 6건 가중치순
- 와이드 열람 모드 ('26.7): 읽기 전용 캘린더(미러 사이트·?view=mirror)만 폭 1560px +
  셀 확대 + 셀당 8건 + 제목 2줄 + **기간 일정 중간 날짜 흐린 연속 표시** + 매체 라벨 병기
  (스탠바이미 등 별도 화면 상시 표시용) + 10분 자동 새로고침(탭 숨김 시 스킵).
  팀용 본 사이트 880px 레이아웃은 불변 — 등록 화면 와이드화는 검토 후 기각
  (입력 동선·미스클릭·환경 편차, '26.7 논의). 팀 화면엔 "크게 보기(새 탭)" 버튼으로 우회.
  간트식 기간 막대는 **보류 확정** — 이벤트 단위는 성수기에 레인 폭발로 기각,
  항목 수가 통제되는 화면(캠페인 단위 등)에서만 재검토
- 알림센터 ('26.7): 탭 바 우측 "새 변경 N" — 지난 확인 이후 **남이** 바꾼 일정
  (`src/NotifyCenter.jsx`, 설계 docs/notify-center-design.md). 확인 시점은 localStorage,
  패널 열람=확인, 0건·이력 미설정·로컬 모드에선 숨김. 행 클릭 딥링크는 2차 예정
- 일정↔실적 매칭 ('26.7): 일정 모달 "집행 실적 후보" — `src/lib/perf.js`가 SNS 수집분을
  채널·게시시점(±오차)으로 근사 매칭 (후보 최대 3건, 근접순).
  **제목 대조 자동 확정 ('26.7.30 — 사용자 지시 "꼭 선택해야 하는지")**: 날짜만 보면 같은 날
  올라온 다른 게시물이 섞이므로 일정 제목·캠페인명과 캡션·영상 제목의 겹치는 글자 조각을
  센다. 한국어는 조사가 붙어 단어가 그대로 일치하지 않아(팝업스토어를 vs 팝업스토어)
  토큰을 2글자 조각으로 쪼개 비교하고, 채널·포맷 지칭(인스타·릴스·촬영·더현대 등 STOP)은
  어느 게시물에나 나오므로 제외. 겹침 2개 이상이면서 2등보다 앞서면(`pickAuto`) 자동 확정 —
  모달은 그 건만 보이고 나머지는 "다른 후보 N건" 접힘, 애매하면 기존처럼 후보 목록.
  담당자 확정(perfUrl)은 언제나 우선. 유튜브는 "3 weeks ago" 근사값
  기반이라 오차 폭 반영. 인스타는 IG.posts(게시물 단위) 필요 — clean-instagram.mjs가 자사
  계정 게시물을 IG.posts로 내보내게 확장('26.7), **다음 격주 수집부터 채워짐** (그전엔 유튜브만
  매칭, 인스타 섹션 자동 숨김). 세부→계정 매핑(perf.js YT_KEY·IG_HANDLE)에 없는 채널은 미노출
- 외부 공유 뷰(`?view=external`)에서는 캘린더 탭·내용 완전 숨김 (내부 일정 비공개)
- **공유 거버넌스 ('26.7 확정)**: 팀원=본 사이트+개인 계정 / 사내 타 팀=미러 사이트(로그인無) /
  외부(대행사·지점)=미러의 `?view=external`(+`&media=매체명` 개별 스펙 딥링크, 스펙 탭
  "이 매체 외부 링크 복사" 버튼으로 생성, MIRROR_URL은 config.js). **외부인에게 계정 발급
  금지** — 뷰어 계정도 로그인만 하면 RLS상 일정 읽기가 가능해 캘린더가 새는 구멍이 됨.
  팀원용 가이드 docs/team-guide.md · 장애 대응 docs/runbook.md
- 공휴일·휴점일 표시 ('26.7): 월간 그리드 셀 강조 (`src/data/holidays.js`). **공휴일**(HOLIDAYS,
  '26·'27 정적·대체공휴일 반영) = 중립 그레이 틴트 + 이름(빨강 금지 원칙). **휴점일**(CLOSED_DAYS,
  백화점 월 1회 휴점 — 확정 시 한 줄 추가) = 클레이 틴트 + "휴점" 마커로 더 뚜렷. 세 캘린더
  탭·미러 공통, 하루 시트 제목에도 병기. 8/10 휴점 반영. test-data 5가 날짜 형식 감시.
  '27 음력 기반 날짜·임시공휴일은 파일에 한 줄 추가.
  **UI 휴점 등록 ('26.7)**: 코드 수정 없이 어느 캘린더 탭에서든 한 줄 입력 "8/10 휴점"
  (제목이 휴점/휴점일뿐일 때) → `kind='휴점'` 마커 저장 (media_events 재사용, 새 SQL 불필요).
  전 탭 셀 틴트 + 하루 시트 "휴점 해제"로 삭제 (정적 CLOSED_DAYS는 코드값이라 해제 버튼 없음).
  휴점 이벤트는 일정 행에서 제외(셀 마커 전용). CalendarApp closedDays/closedEvt 병합
- **팀즈 아침 브리핑 ('26.7 확정)**: `.github/workflows/notify.yml` — 평일 09:00 KST 정각
  ('26.7.27 사용자 지정) 팀즈
  채널로 요약 카드 3섹션 (① 오늘 팀원 일정 = kind='팀', 기념일은 월-일 일치 ② 오늘 촬영 일정
  ③ 오늘 업로드 일정) + 하단 "미콘룸 바로가기" 버튼(홈으로 연결, '26.7.29).
  **미발송 사고 ('26.7.24 → 7.27 재발, 진범 확정)**: 7/24는 "00:00 UTC 혼잡"으로 진단하고
  크론만 옮겼으나 7/27 또 미발송 → 실행 기록 전수 대조로 진범 발견: **앱 토큰(Claude 통합)
  push로는 GitHub이 크론 변경을 재등록하지 않는다.** 물증 = 최초 크론(`0 23`)은 지연 발화라도
  했고(7/22 23:57Z, 당시 파일은 이미 새 크론), 이후 변경한 크론('0 0'→'52 23')은 3슬롯 연속
  무발화. **조치**: ① 크론 이중화 — 본편 `0 0 * * 1-5`(09:00 KST 정각, '26.7.27 사용자 지정) +
  백업 `25 0 * * 1-5`(09:25 KST), 백업은 "오늘 KST 성공 발송(이벤트 무관) 있으면 생략" 가드
  (gh api dedup, permissions actions:read. '26.7.28 확장 — 지연 크론이 안전망 dispatch 뒤에
  와도 중복 방지) ③ **Anthropic 루틴 안전망 ('26.7.28)** — 평일 09:06 KST에 이 세션이
  자동으로 깨어나 오늘 발송 여부 확인, 없으면 workflow_dispatch (GitHub 크론 불신 대응 —
  크론이 2주 연속 정상이면 루틴 제거 검토) ② **크론 변경은 반드시 GitHub 웹 UI에서 사용자 본인 커밋으로**
  (재등록 트리거 — notify.yml 상단 주석에도 명기). UTC 23시대 = KST 다음날이라 요일 하루
  앞(일~목 = KST 월~금). daily-brief.mjs는 KST 보정(kstNow)이라 시각 무영향.
  누락 시 복구 = Actions에서 notify.yml Run workflow 수동 실행(ALWAYS_SEND 자동). **RMN·정산·휴점 등 기타 항목은 전부
  제외** ('26.7 사용자 확정 — 이전 RMN 섹션·버튼 구성을 대체).
  `scripts/notify/daily-brief.mjs` — 없는 섹션 숨김·전부 비면 발송 생략(ALWAYS_SEND=1로 강제).
  카드 문구도 문자열 규칙 적용('26.7.29 — 중점·em dash 제거, 매체는 괄호 표기).
  웹훅 = Power Automate 워크플로(docs/teams-webhook-setup.md, 구 Incoming Webhook 커넥터는
  MS 폐지 — 워크플로 재생성 시 TEAMS_WEBHOOK_URL 시크릿 값만 교체하면 됨).
  **시크릿: TEAMS_WEBHOOK_URL(필수) · SUPABASE_SERVICE_KEY(권장 — 일정 조회용)**.
  검증: --mock --dry-run. 샌드박스는 powerplatform.com·supabase.co 아웃바운드 차단 —
  실발송 검증은 Actions에서만 가능
- 데이터 자동 백업 ('26.7): `.github/workflows/backup.yml` — 주 1회(월 05:43 KST) 일정 전체를
  `data/backup/media-events.json`으로 커밋 (git 이력 = 시점별 복원점). 인증: anon 키(미러 읽기
  정책 필요) 또는 `SUPABASE_SERVICE_KEY` 시크릿(이력까지 백업). 0건 응답이면 저장 안 하고 실패
  처리 (빈 덮어쓰기 방지). 수동: Actions Run workflow 또는 `npm run backup`
- SNS 지표 추이 ('26.7): 수집 시마다 핵심 지표 스냅샷을 `src/data/sns/trend.js`에 누적
  (append-trend.mjs, 최대 60개). 모니터링 탭 팔로워·구독자에 직전 수집 대비 증감(▲그린/▼그레이)
  표시 — 스냅샷 2개부터 보임 ('26.7.7 시드 완료, 다음 수집부터 표시)
- **어드민 페이지 ('26.7)**: `#admin` 탭 — config.js `ADMIN_EMAILS` 계정만 탭 노출+렌더
  (현재 규빈·김자영 책임, 로컬 테스트 모드는 항상 허용). `src/AdminPage.jsx` ① 일정 일괄 관리(검색·
  구분·기간 필터 + 체크박스 다중 선택 → 일괄 삭제(2단계)·캠페인 일괄 변경, 상위 200건 표시)
  ② 최근 90일 삭제 기록 원클릭 복원(이력 테이블 기반, 새 일정으로 재등록) ③ 타겟APP 실적
  입력 폼(매월 수기 — targetapp_stats CRUD, 매체 체크박스·datalist 사업소, 수정·삭제 포함).
  **항목별 접기 ('26.7.29)**: 세 항목 모두 제목 줄 클릭으로 접기·펼치기(AdminPage
  AdmSection, "펼치기 +/접기 −" 타이포 어포던스). 기본은 ①만 펼침, ②③은 접힘.
  UI 게이트는 편의일 뿐 실제 차단은 RLS(team_writers) — 새 SQL 불필요. team_writers 관리
  UI는 미구현(관리자용 RLS 정책 설계 필요, 당분간 SQL로)
- **타겟APP 실적 엑셀 업로드 ('26.7 v2 — 소스 파일 교체)**: 어드민에서 **"26년도 타겟형 매체
  광고 실적" 대장(.xlsx)**을 그대로 업로드 (구 "(결과)" 내부 정리 문서 양식은 잘못된 소스로
  확인되어 파서 폐기). `src/lib/parseTargetXlsx.js` — 시트명 "{N}월_{사업소}(종료|진행중)"만
  파싱(전체/Start/End 제외), B2 연도·C4 "캠페인명 (기간)"·헤더 라벨(예산/비용/노출/클릭/방문/
  앱다운로드)로 컬럼 탐지, !ref가 B2에서 시작해도 절대 좌표로 읽음. 매체 행 = C열 매체명,
  D열 매체별 기간 우선, 실적·비용 전부 0인 행은 미집행 템플릿으로 제외 → 미리보기(진행중
  플래그·동일 복사본 체크 해제) → 선택 반영. **매체별 1행 분할** + 노출·클릭·방문·앱다운로드·
  예산·비용 저장(budget/cost 컬럼은 setup.md 7-1 ALTER 필요, 값 있을 때만 전송).
  upsert 키 = 사업소+매체별기간+단일 매체 — 재업로드 시 파일 값으로 갱신하고 note만 보존.
  **"전체 삭제 후 반영" 체크 = 대체 업로드**(targetappStore.deleteAllTargetApp).
  파일은 브라우저에서만 읽음(사진 포함 원본 외부 전송 없음), SheetJS 동적 import(별도 청크).
  실물 300MB급(이미지 포함)은 PC 크롬 권장 — 파싱은 데이터만 읽지만 메모리 수백 MB 소요 가능,
  실물 검증은 미완(샘플 30MB 검증 완료)
- **RMN 탭 ('26.7 1차 — GA 연동 전)**: `#rmn`, `src/RmnPage.jsx` — 현대백화점 APP 광고
  판매(부킹·재고·정산) 관리. 팀 전체 노출(내부 로그인), 데이터는 Supabase 전용
  `rmn_bookings`(RLS — data/rmn-setup.sql, setup.md 8장, **사용자 액션: SQL 1회**).
  상품 7종·공시가는 `src/data/rmn.js`(공개 단가라 번들 OK, 부킹 데이터는 DB만).
  재고: 구좌 상품 = 기간 겹침일 동시 점유 ≤ 구좌 수, 푸쉬 = 발송 1회(일자)당 90만 건
  ('26.7 확정) · 5만 단위 판매(건당 50원) · 발송 일자+시간 필수.
  **상품별 독립 라인 ('26.7 재설계)**: 상품을 여러 개 선택하면 각 상품이 **기간·수량·할인율·
  가격을 따로** 가짐(RmnPage lines 상태 — 상품마다 라인 카드). 하단에 **합산 바**(총 공시가·총
  광고비·입금가). 상품별로 집행 기간이 달라도 됨(스플래시 7일 + 메인배너 21일 등). 저장은
  상품별 부킹 행(각자 start/end·discount·price). 이전 "공유 기간 + 7일 분할" 모델을 대체.
  **캠페인 기간 일괄 + 개별 오버라이드 ('26.7)**: 폼 상단 "캠페인 기간"으로 구좌 상품 기간을
  일괄 지정(own=false 라인이 추종) — 라인 날짜를 직접 고치면 그 상품만 개별 기간(own=true,
  "캠페인 기간으로" 버튼으로 복귀). 푸쉬·카카오·인스타는 자체 일자 사용.
  **필수 미입력 표기 ('26.7)**: 미입력 필드에 빨간 테두리(rmn-err) + 부킹 등록 버튼 좌측에
  "미입력: 광고주명 · …" 실시간 안내(missing 배열). 광고주명·발송일/시간·발송건수·시작일·게시일 등.
  **기간 비례 과금 ('26.7)**: 구좌 단가는 7일 기준 — 21일이면 **×3**, 비 7일 배수는 **무조건
  올림**(10일=×2, rmn.js priceWeeks = ceil(days/7), 최소 1). 시작일 입력 시 종료일 자동 +6(1주).
  라인에 "N주" 배지. 집행일이 과금 주(7×N일)에 미달하면 "잔여 N일" 안내 + **"＋ 추가 일정
  선택"**으로 라인별 분할 세그먼트(잔여일을 다른 일자에) — 세그 행은 금액 0으로 저장(재고만
  점유). **할인율 ↔ 실판가 양방향 동기**: 할인율 입력 → 실판가 자동, 실판가 입력 → 할인율
  역산(0.1% 단위 소수 유지 — '26.7 확정). 합산 바 총 광고비 옆에 **총 할인율**(공시가 합
  대비 실판가 합의 실효 할인율 — 상품별 나열 아님, '26.7 확정: 메인10%+팝업20% → 13%).
  **레이아웃 ('26.7)**: 부킹 캘린더가 탭 최상단, 상품 선택은 신규 부킹 폼 안으로 통합.
  **상품 추가 ('26.7)**: ⑧ **카카오톡** — 발송형 건당 100원(**발송 건수 직접 입력**), **타겟팅 체크 시 10% 할증**
  (rmn.js kakaoPrice, 발송 한도 미확정이라 재고 미표시) ⑨ **인스타그램** — 게시형
  구성 4종(1회 업로드/스토리 동시/상단 고정 7일/3피드+스토리 3회) × 형식(피드 이미지·영상)
  단가표(INSTA_PRICES — 200~1,200만 원, 사용자 제공 단가). 라인 = 구성·형식 select +
  게시일(상단 고정만 자동 7일 기간). 세부는 `option` 컬럼 저장(값 있을 때만 전송 —
  **setup.md 8-2 ALTER 1줄** 필요, 미실행 시 카카오 타겟팅·인스타 저장만 실패). 기존 7종 무영향.
  **상품 수량 ('26.7)**: 구좌 2개 이상 상품은 라인에서 수량 스테퍼(팝업배너 3개 등) — 재고는
  수량 합으로 점유(bookingQty·slotAvailability). qty 컬럼은 값>1일 때만 전송(하위호환) —
  수량 판매는 **setup.md 8-1 ALTER 1줄** 필요(단일은 불필요).
  **캠페인 그룹핑 ('26.7)**: 목록·캘린더·상태를 상품별이 아닌 **[광고주+캠페인명]** 캠페인 단위로
  (rmn.js groupCampaigns — 이름 있으면 기간 갭 무관 한 캠페인, 무명만 갭>3일 회차 분리, 취소 제외).
  진행중 = 캠페인 그룹 행(클릭 시 세부 상품 펼침), 상태 "다음 →"·select는 캠페인 전체 일괄 진행
  (가장 덜 진행된 상태 기준, 혼합이면 "상태 혼합" 표시). 캘린더 = 캠페인 칩(상품 색점+광고주,
  클릭 시 상품 선택 시트 → 개별 수정). 청약서도 캠페인 전체 상품 한 장. test-data 6d 감시.
  **상품별 이미지 첨부 ('26.7)**: 캠페인 펼침의 상품 행마다 "이미지 N" 버튼 → 패널(한 번에
  하나만 열림 — 붙여넣기 대상 명확화). ImageAttach 공용 위젯(자동 압축·라이트박스·Ctrl+V),
  파일은 event-images 버킷 `rmn/{부킹id}/` 경로(10장 버킷 공용), 메타는 rmn_bookings.images
  jsonb (**setup.md 8-3 ALTER 1줄** — 미실행 시 이미지 저장만 실패). updateRmn 부분 PATCH라
  일반 수정이 첨부를 안 덮음. RMN 탭 자체는 미러에 없음(내부 전용 불변) — 이미지 공개 수준만
  일정 첨부와 동일. **대장 0723 v1 검증 ('26.7)**: 최종본 대장(2025 시트 표기 정리본)을
  재파싱해 기존 rmn-seed.sql과 124건 완전 동일 확인 — 재적재 불필요, 합계 동일. 수익 수식은
  원본 자구 그대로(`IF($D:$D="직거래", ...)` 열 참조) buildLedgerXlsx에 반영.
  **완료·취소도 펼침·수정 ('26.7)**: 완료 캠페인은 진행중과 동일한 CampaignRow(펼쳐서 상품별
  수정·삭제·상태 변경, select로 완료→이전 단계 되돌리기 가능, **연도 배지 showYear**). 재적재로
  완료 건 다수라 완료·취소 섹션 상단에 검색창(광고주·캠페인·상품). 취소 건은 개별 행(수정·삭제,
  연도 배지). 완료 상태 캠페인은 "다음 →" 숨김.
  **정산 요약 연도별 + 실적 대장 엑셀 ('26.7 3차)**: SettleSummary에 연도 필터 칩(전체/2025/2026)·
  연도별 소계 행(전체 선택 시). **"실적 대장 다운로드" = 원본 판매실적 대장 양식·수식 그대로 재현**
  ('26.7 확정 — 단순 월별 요약 다운로드를 대체): `rmnDocs.buildLedgerXlsx`가 부킹을 캠페인(소계)
  단위로 묶어 **연도별 시트**(2025 먼저 — YoY 참조), 소계=SUM·합계=SUMIFS($F,"소 계")·할인율=
  IFERROR(L/J-1)·매출=SUM(M:N)·수익=IF($D="직거래",$M,$M*0.7)·YoY=전년 시트 참조까지 **수식 포함**.
  **블록 그룹핑 = ledgerGroups ('26.7.28 — "2025 데이터 오류" 수리)**: 화면용 groupCampaigns
  (이름 병합)가 아니라 created_at 등록순 인접 그룹핑 + **회차 분리**(같은 키 연속이라도 새 항목
  시작일 > 현재 블록 종료일이면 별도 블록 — 원본이 같은 캠페인명을 회차별 블록으로 적는 관례,
  '26 벨라 1~3월 3블록. 블록 기간 내 시작일 상이(팝업 7/15+푸쉬 7/19)는 한 블록 유지).
  상세 상품명은 ledName — 복수 구좌 "(3구좌)"·발송형 "20만명" 병기. v1_1 대장과 전수 대조
  '26.7.28: 블록 2025 16/16·2026 21/21, 금액 불일치 0 (유일 차이 = 소계에만 금액 있던
  벨라 2140을 첫 상세 행에 실은 시드 관례 — 의도된 매출 보존).
  회계 서식·할인율 %·날짜 m/d(aaa)·열 병합(헤더 4:5, 캠페인 블록 B/C/E/R/S/T)·헤더 고정 원본 대조
  (test-data 6f = DOC_ORDER 전 상품 커버 + export 감시). 취소 건 제외, 금액 천원(원/1000).
  RMN_판매실적대장_날짜.xlsx (ExcelJS 동적).
  판매사(나스미디어 등 5사+직접입력) 선택 시 수수료 30% 차감 입금가 분리 표시 +
  담당자·연락처·이메일 입력(사업자번호·주소는 연동값 대기).
  상태: 가부킹→부킹→집행→결과 리포트→세금계산서→입금 확인→완료(+취소, 취소만 재고
  해제) — 목록에서 select·"다음 →" 원클릭. **가부킹 = 시작일 3개월 초과 남았을 때만**
  ('26.7 확정, 자동 해제 없음) — 3개월 이내 진입 가부킹은 탭 접속 시 "부킹 전환" 팝업
  (하루 1회) + 월말 5일 전부터 세금계산서 미교부 리스트 팝업 (data/rmn.js
  buildRmnNotices — test-data 6d 단위 테스트). 부킹 캘린더 = 간이 월간 그리드(상품
  이니셜 흑백 칩, 가부킹 = (가) 회색). **정산 요약 ('26.7 2차)**: 진행 목록과 부킹
  캘린더 사이 — 월별(시작일 기준) 건수·총광고비·입금가·미수금(취소 제외, '입금 확인'
  미도달 건의 입금가 합) 테이블 + 합계 행 (RmnPage SettleSummary). **판매 실적 이관 SQL =
  `data/rmn-seed.sql`** ('26.7 대장 전체 재적재 — "2026.06 외부 광고 판매 실적" 대장(0723)
  기준, **2025 48건 + 2026 76건 = 124건**. 맨 위 `delete from rmn_bookings;`로 기존 전체
  삭제 후 재적재 — **사용자 액션: Supabase SQL Editor 1회 실행**). 대장 천원→원(×1000),
  매출(actual_price) 합 = 대장 합계 2025 313,045천·2026 521,403천 정확 일치. 수익(net_amount)은
  카카오톡 발송비 조정분(샤넬·피아제)·삼성닷컴 등 대장 자체 소계≠상세 건은 상세 값 그대로
  적재라 합계 대비 소폭 차이(대장 원본 반영, SQL 헤더 주석). 소계에만 금액 있던 2건은 첫 상세
  행에 실어 매출 보존. 상품명은 DOC_NAME 역매핑(앱 오픈 팝업→팝업배너, (3구좌)→qty, (s)/만명
  →푸시·카카오), 카탈로그 외(사이니지 등)는 원문 유지. 준비중→부킹, 그 외 완료.
  구버전 rmn-seed-2025.sql·rmn-fix-2025-status.sql은 이 통합 시드로 대체·삭제. 행 수·delete
  선행·날짜 형식은 test-data 6e가 회귀 감시.
  **GA4 연동 가동 ('26.7.28)**: 권한·키 확보 완료 → `scripts/rmn/ga4-collect.mjs` +
  `.github/workflows/ga4-collect.yml`(매일 06:40 KST + 수동 mode: collect/discover/dry-run/
  backfill). 서비스 계정 JWT 직접 인증(의존성 0), 파라미터 실명 discover로 확정 —
  구좌 `customEvent:ep_ad_location`(통합앱_스플래시 등) · 광고주 `customEvent:ep_ad_agency` ·
  view_ad/click_ad. 부킹별 [광고주×기간×구좌] runReport → impressions/clicks PATCH
  (**setup.md 8-4 ALTER 2줄** 필요). 일일 수집은 최근 30일 창, 과거분은 backfill 1회.
  dry-run 검증 '26.7.28: 대상 94건 중 60건 실데이터 매칭, backfill로 실적재 완료.
  RMN 상품 행에 노출·클릭·CTR 그린 표기(rmn-item-ga). 공개 리포라 Actions 로그에 실적
  수치 미출력 원칙(건수만). **광고주명 유사어 자동 매칭 ('26.7.28 사용자 승인)**: 수집 시
  GA 광고주 표기 전체를 1회 조회 → 대장 표기와 발음 정규화(경음·격음→평음: 까르띠에↔
  카르티에·까르디에) + 글자 순서 뒤바뀜(로베에↔로에베) 매칭, 매칭되면 GA 실표기로 정확
  조회(inListFilter)·못 찾으면 CONTAINS 폴백. 구 --audit 모드(팀즈 발송)는 폐기.
  **일별 적재 + 결과보고서 ('26.7.28)**: collect가 rmn_ga_daily(setup.md 8-5, 광고주×구좌×일)
  upsert(테이블 미설정 시 스킵) → RMN 캠페인 행 "결과보고서" 버튼(GA 데이터 있는 캠페인만)
  = rmnDocs.buildResultXlsx가 부쉐론 양식(public/templates/rmn-report.xlsx — BT/BU/BV 입력존
  실데이터 소거본, 14일 이내 캠페인 전용)에 일별 채움, CTR·CPM·CPC는 Excel 수식 자동.
  크론 활성화 = 웹 커밋 필요(notify.yml 사고 참조). 상세 = docs/rmn-ga4-plan.md.
  **RMN 문서 자동 생성 ('26.7 3차)**: ① **청약서** — 진행/완료 목록 행 "청약서" 버튼 →
  같은 광고주·기간 겹침 형제 부킹(≤6행)을 한 장으로 다운로드. 판매사 회사 정보는
  `src/data/rmnAgencies.js` RMN_AGENCY_INFO(법인 공적 정보만 — 담당자 연락처는 부킹
  레코드에서, 개인정보 번들 금지 원칙. 나스미디어 등 4사 사업자번호·주소 미확보 → 빈칸,
  확보 시 한 줄 채움) ② **제안서** — "캠페인 제안서 만들기" 접힘 폼(광고주·기간·할인율·
  상품 ≤4개·푸시 발송량) → 미디어 믹스 자동 산출. 예상 노출·CTR은 RMN_BENCH
  (**까르띠에 9월 제안서 수치 기준** — '26.7 사용자 확정, 변동 시 rmnAgencies.js 수정),
  기준값 없는 팝업·헤드라인·이벤트 메뉴는 "-" 표기(추정치 날조 금지) ③ **결과보고서** —
  `scripts/rmn/build-report.mjs` (GA4 파이프라인의 출력 단계, `--mock` 검증 완료 —
  위 GA4 항목 참조). 구현: `src/lib/rmnDocs.js` + 빈 템플릿 `public/templates/rmn-*.xlsx`
  (원본 실데이터 소거본 — public은 공개 URL이라 실데이터 금지, test-data 6f 감시).
  ExcelJS는 동적 import 별도 청크(938KB — 버튼 클릭 시에만 로드). **클로드 API 불필요
  결론** ('26.7 검토 — 전부 결정적 템플릿 채움, 토큰 비용 0)
- **RMN 광고주 공유 리포트 ('26.7.29)**: 캠페인 결과 대시보드를 광고주에게 링크로 공유 —
  RMN 캠페인 행 "광고주 공유" 버튼(GA 데이터 있는 캠페인만) → ShareModal(RmnPage)에서 발급.
  **격리 = 스냅샷 방식**: 발급 시점에 해당 캠페인 데이터만 jsonb로 `rmn_share`에 저장 —
  광고주는 원본 rmn_bookings/rmn_ga_daily에 접근 불가(anon 정책 없음), 열람은 security
  definer RPC 2종(get_rmn_share_meta/get_rmn_share — 토큰+비밀번호+미만료 전부 일치 시만)뿐.
  **30일 만료**(RPC단 강제) + **임시 비밀번호 8자**(genPw, 0/O/1/l 제외, 기본 on) +
  **금액은 기본 미포함**(withMoney 옵트인 — 포함 시 광고비·CPM·CPC 병기, 수수료 구조는 항상
  미노출) + 회수 = 발급 목록에서 행 삭제. RMN_BENCH(타 광고주 기준치)는 공유 페이지 미사용.
  발급 목록에서 **기존 발급분의 링크와 비밀번호를 다시 확인**할 수 있다 ('26.7.29 — 광고주가
  링크를 잃어버렸을 때 재발급 없이 같은 링크를 다시 보내려고. 읽기 전용 입력 + 링크 복사 버튼).
  진입 = `?share=<token>` — App.jsx **로그인 게이트보다 먼저** 분기(src/ShareReport.jsx,
  비번 게이트 → 대시보드). 내용 = 엑셀 결과보고서 항목(일별 노출·클릭·CTR) + KPI 4장 +
  하이라이트 자동 문장(최고 노출일·최고 CTR일·구좌 비중) + 일별 막대·구좌 도넛·구좌별
  성과·요일 평균 + 일별 상세 표 — 전부 스냅샷 실데이터 계산, 날조 없음. 엑셀 양식의 14일
  제한과 달리 기간 제한 없음.
  **GA 미측정 매체 = 발급 시 수기 입력 ('26.7.29 사용자 지시)**: 카카오톡·푸쉬·인스타그램은
  GA4가 집계하지 못함 → 발급 모달에 **"매체 실적 입력"** 섹션이 자동으로 뜨고, 매체사 리포트
  (카카오 비즈메시지 관리자·인스타 인사이트) 수치를 입력받아 리포트에 함께 싣는다. 필드 정의는
  rmn.js `MANUAL_PERF` (카카오톡 발송 성공·클릭 → 클릭률 / 푸쉬 발송 성공·오픈 → 오픈율 /
  인스타 도달·노출·참여 → 참여율, rate = 마지막÷첫 값). 입력값은 **렌더 완료형**(rows·rate·
  reach·act)으로 스냅샷에 저장 — 나중에 필드 정의가 바뀌어도 과거 리포트는 그대로. 동시에
  rmn_bookings.perf_manual에도 저장해 재발급 때 프리필(**setup.md 8-6 ALTER 1줄**, 미실행 시
  프리필만 안 됨). 리포트에는 **"발송·게시 매체 실적"** 표(매체/집행 내역/실적)로 표기하고
  비우면 "집계 확인 중" — 추정·환산은 하지 않음. **발송·게시형만 집행한 캠페인도 공유 가능**
  (공유 조건 GA 실적 → "집행 이후 상태" 완화 = canShare), 이 경우 KPI가 집행 매체·총 도달·
  총 반응·(옵트인)광고비로 바뀌고 성과 차트·일별 표는 숨김. 푸터 출처도 GA4/매체사 리포트 분기.
  **PDF 저장** = 헤더 우측 버튼 → `window.print()` + `@media print`(글래스·그림자 해제,
  패널 break-inside:avoid, print-color-adjust:exact) — 라이브러리 0, 한글 폰트 그대로. 파일: src/lib/shareStore.js·ShareReport.jsx·RmnPage ShareModal·
  data/rmn-share-setup.sql (**사용자 액션: SQL 1회 — setup.md 12장**, 미실행 시 발급만 실패)
- **정산 탭 ('26.7 테스트 — 3인)**: `#settle`, `src/SettlePage.jsx` — 법인카드/세금계산서
  정산 관리. **config.js `SETTLE_EMAILS` 게이트**(노규빈·박준영·한은비만 탭 노출, 전 팀
  오픈 시 목록 확장). 데이터는 Supabase 전용 `settlements` + `settle-docs` 비공개 Storage
  버킷 (**사용자 액션: data/settle-setup.sql 1회** — setup.md 9장, RLS 로그인 읽기·
  team_writers 쓰기, 미설정 시 안내 문구만).
  ① 등록: 유형 분기 — 법인카드(일반=계정과목 select / 불가=간편결재 문서번호), 세금계산서
  (점 배분). 담당자는 로그인 계정 자동(team.js). 등록 직후 행 자동 펼침 → 증빙 첨부
  ② **증빙 첨부**: 유형별 슬롯(영수증·간편결재 / 견적서·계약서·내부 품의), **이미지 업로드
  시 브라우저 자동 압축**(긴 변 1600px JPEG — 폰 사진 3~5MB→~300KB, settleStore
  compressImage, 압축이 크면 원본·HEIC 실패 시 원본), 파일당 10MB 상한. 실파일 Storage,
  메타는 settlements.files jsonb
  ③ **점 배분**: `src/data/settle.js` ALLOC_PRESETS — 백화점(13개점·법인 73.6/26.4) /
  전사 아울렛 포함(24개점·68.5/31.5), 분담률 소스 = 사용자 정산 엑셀('26.7. 원본 시트명은
  "14개점"이나 실제 13개 행). 점 체크 해제 시 잔여 비율 재정규화(합 100%), 원 단위 잔차는
  최대 점 보정(합=정산액 정확 일치, test-data 8 — 본점 1,910,250원 원본 대조). 배분표
  xlsx 다운로드(ExcelJS 동적)
  ④ **상태 파이프라인**: 법인카드 작성→증빙 완료→전표 처리→완료 / 세금계산서는 +계산서 발행
  단계. 3인 전원 공유 뷰 — 상단 "증빙 미첨부 N건·계산서 미발행 N건" 요약(담당 이름 나열,
  클릭=필터), 필터 칩 5종
  ⑤ **반복 정산 템플릿**: recurring 플래그 — 매달 같은 파일 재사용(템플릿에 첨부, 언제든
  다운로드), "이번 달분 생성" = 파일 참조 복사(재업로드 불필요. 단 템플릿 삭제 시 파일도
  삭제되어 참조 복사본 링크 깨짐 — 테스트 단계 한계)
  ⑥ **ZIP 일괄 다운로드**: 월 선택 → `귀속월/등록일_제목_담당자/파일` 폴더링.
  `src/lib/zip.js` 자체 무압축 ZIP 생성기(의존성 0, UTF-8 한글 파일명, unzip·python 검증,
  test-data 8b 회귀). 회기 마감 백업 → 지난 건 삭제로 Storage 1GB 관리 (setup.md 9장)
  ⑦ **홈 배지**: SETTLE_EMAILS 계정만 — 내 미첨부 건 있으면 홈 상단 "증빙 첨부 필요 N건"
  (HomePage SettleBadge, 동적 import라 비대상 계정 비용 0) ⑧ 팀즈 아침 브리핑에
  "정산 증빙 미첨부" 섹션 추가(daily-brief — 테이블 미설정 시 조용히 생략).
  계정과목 목록(SETTLE_ACCOUNTS)은 임시 5종 — 실목록 사용자 전달 대기
- **노션 캘린더 동기화 ('26.7 — 단방향)**: 대행사 공용 "업무 요청 현황" DB를 매체
  캘린더로 (`scripts/notion/sync-notion.mjs` + `.github/workflows/notion-sync.yml` 매시 17분).
  전 행 = 인스타('26.7 사용자 확정 — 매체 필터 없음). **"업로드 일정" 날짜 → 매체 캘린더,
  "촬영*" 날짜 → 촬영일정 탭(kind='촬영', upsert 키 {pageId}#shoot)** — 한 행 최대 2건,
  날짜 없는 행 스킵. 매핑 = 제목·기간·캠페인(속성명에 '캠페인' 포함 시), 작성자 "노션" +
  메모에 원본 링크.
  upsert 키 = media_events.notion_id — **갱신은 제목·기간만** (우리 쪽에서 붙인 캠페인
  변경·메모·이미지 보존). **노션에서 지운 일정은 자동 삭제 안 함** ('26.7 사용자 결정) —
  notion_gone 마킹 → 매체 캘린더 상단 검토 배너(config.js NOTION_REVIEW_EMAILS = 규빈만)
  에서 건별 [삭제(2단계)/유지(연동 분리 = notion_id 제거)]. store.resolveNotionGone.
  **사용자 액션: setup.md 11장** (SQL 컬럼 2개 + 노션 통합 생성·DB 연결 + NOTION_TOKEN
  시크릿 + 크론 웹 커밋). 검증 = --mock --dry-run (data/notion-mock.json, test-data 10 회귀).
  노션에서 날짜를 지운 행도 gone 처리됨 — 정상 동작
- 캘린더 디자인: Wanted DS 참고 — 뉴트럴 그레이 스케일(--g50~--g900), 정돈된 컴포넌트.
  포인트 컬러는 현대그린 유지 (Wanted 블루 미사용)

## 사용자 (규빈)
- 현대백화점 본사 미디어콘텐츠팀 책임. 광고 업계 장기 경력, 심미 기준 명확.
- 협업 방식: 결론 선행형. 지시된 구조를 정확히 반영하고 임의 재구성 금지.
- 기술 안내는 클릭 수준의 단계별 지시로.
- 답변 끝에 후속 질문 3개 제안.

## 스택 (확정)
- React + Vite, 배포 Vercel (사용자의 표준 스택 — SIC Calendar와 동일 구조)
- 데이터: `src/data/media.js` 단일 파일. CMS·DB 없음. 수정 = 파일 편집 → git push → 자동 배포
- 프로토타입(`prototype/index.html`)이 디자인·UX의 확정 기준. React 전환 시 시각 결과물이
  프로토타입과 동일해야 함. → **'26.7 React 전환 완료** (사용자 확인 후 진행). 프로토타입은
  디자인 레퍼런스로 보존, 이후 수정은 `src/` 기준.

## 외부 디자인 스킬 (taste-skill, '26.7.29 설치)
- `github.com/leonxlnx/taste-skill`(MIT)의 Agent Skills 13종을 `.claude/skills/`에 설치.
  프론트엔드 심미 보조용 — 랜딩·포트폴리오·리디자인 지향. 주요 스킬:
  `design-taste-frontend`(메인) · `image-to-code`(스크린샷→코드) · `redesign-existing-projects` ·
  `brandkit` · `minimalist-ui` · `high-end-visual-design` · `stitch-design-taste` · imagegen 2종
- **우선순위: 아래 "디자인 규칙"이 항상 우선.** 스킬이 제안하는 컬러 확장·그라데이션·
  그림자·Tailwind/GSAP 도입은 이 프로젝트에 적용하지 않는다 (스택도 React+Vite 수작성 CSS).
- 스킬 자체가 "dashboards·data tables·multi-step product UI에는 쓰지 말 것"이라고 명시 —
  이 앱의 본체(캘린더·표·폼)에는 부적합. **채널 성과 덱 같은 발표용 화면**이나 새 랜딩성
  페이지에서 아이디어 참고용으로만 호출할 것
- 갱신: `npx skills add https://github.com/Leonxlnx/taste-skill` (로컬) 또는 리포 재복사

## 로고 ('26.7.29 — 사용자 제공 가이드 적용)
- 원본 SVG 8종은 `public/logo/`, 파비콘은 `public/favicon.svg` (그린 배경 + 화이트 심볼,
  라운드 20/128). 화면에서는 **인라인 컴포넌트 `src/Logo.jsx`**로 그림 — 네트워크 요청 0,
  `color` prop으로 그린과 화이트 전환 (다크 무대 재사용)
- 구조: 사각형 30개로 짠 기하 레터링. 전각 128 정사각, 획 14, 심볼과 워드마크 간격 44,
  글자 사이 14 — **뷰박스 좌표 고정, 임의 조정 금지**
- 배치: 로그인 화면 = 가로 조합 52px / **사이드바 = 심볼 22px + 사이트명**(사이드바 폭에서는
  가로 조합이 최소 크기 48px를 못 맞춰 가이드대로 심볼 단독) / 미러 상단 필 바 = 심볼 18px /
  채널 덱 표지 = 화이트 가로 조합 30px. 광고주 공유 리포트는 THE HYUNDAI 표기 유지
  (미콘룸은 내부 도구 브랜드). **광고주 공유 리포트 헤더 = THE HYUNDAI 워드마크**
  (`public/logo/the-hyundai.svg` — 사용자 제공 PDF를 pdftocairo로 SVG 변환 후 여백 트림,
  잉크색 고정. img 참조라 번들 영향 0)
- 금지: 그라데이션·그림자, 비율 변경, 획 두께 조정, 심볼 개방부 폐쇄, 그린 외 임의 컬러,
  48px 미만에서 가로 조합 사용

## 홈 통합 검색 · 스펙 최근 집행 · 중복 등록 확인 ('26.7.29 사용자 지시)
- **통합 검색**: 홈 헤더 우측 하나 (`HomePage GlobalSearch`, `.gs-*`) — 일정, 매체 스펙,
  RMN 부킹을 한 번에. 일정과 스펙은 이미 로드된 데이터, **RMN은 첫 검색 때만 동적 import**
  (홈 초기 로딩 비용 0). 결과 클릭 = 해당 탭 이동, 스펙은 `onOpenSpec`으로 딥링크 포커스까지.
  바깥 클릭이나 Esc로 닫힘
- **스펙 최근 집행**: 스펙 상세 하단에 이 매체로 등록된 일정 최근 5건 (`RecentRuns`).
  캘린더에서 스펙으로 가던 딥링크의 역방향 — 매핑은 `specLink.js eventMatchesMedia`
  (SPEC_LINK_MAP 재사용, 타겟APP은 세부명이 곧 매체명). **내부 뷰 전용** — 외부 공유·미러는
  내부 일정을 노출하지 않으므로 렌더 자체를 건너뜀
- **중복 등록 확인**: 빠른 입력 등록 시 같은 날짜·같은 매체에 제목이 사실상 같은 일정이
  있으면 등록 전 확인 시트에 기존 건을 보여준다 (`CalendarPage findDuplicates`).
  제목 비교는 공백·기호 제거 후 완전일치 또는 포함관계(짧은 쪽 4자 이상). 막지 않고
  보여주기만 함 — 그대로 등록 가능. 대신 등록이 잦은 팀 특성상 이중 등록 방지용

## 곡률과 세그먼트 ('26.7.29 2차 — 사용자 지시)
- **컨트롤 곡률 18px** (버튼·입력·셀렉트, 알약 직전 값. 22px은 높이 42px 입력이 완전 알약이 됨),
  작은 상태 칩 12px, 날짜 숫자·미니먼스 네비 10px, 세그 컨테이너 20px / 알약 16px.
  index.css 맨 아래 "곡률 통일" 블록 단일 소스 — 신규 컨트롤도 여기 셀렉터에 추가할 것.
  **검색 입력만 예외** (밑줄 스타일 유지 = 등록 입력과 구분, '26.7 확정)
- **세그먼트 글래스모피즘**: 인스타·유튜브 같은 구분 탭이 평면 회색이라 패널 언어와 따로
  놀았음 → 컨테이너 반투명+blur(16px)+inset 하이라이트, 선택 알약은 흰 유리 + 그린 텍스트 +
  그린 틴트 그림자
- **매체 표시 우선순위 ('26.7.29 사용자 확정)**: `data/channels.js` `MEDIA_RANK` 단일 소스 —
  ① SNS(인스타·유튜브) ② 기타(백화점APP·카카오톡·기타) ③ 장기 상시(APT LCD·버스)
  ④ 타겟APP. 상한이 있는 화면(월간 셀·하루 시트·홈 오늘과 내일 게시)에 적용,
  캠페인 뷰는 날짜 흐름이 중요해 미적용. 홈 표시 상한은 6 → **9건**(HomePage HOME_MAX)
- **RMN 결과보고서 엑셀 버튼 제거 ('26.7.29)** — 광고주 공유 대시보드가 대체.
  생성 함수 `rmnDocs.buildResultXlsx`는 보존이라 버튼만 되살리면 복구됨
- **타겟APP 주제 묶음 (하루 시트·홈)**: 같은 제목·기간의 타겟APP을 한 줄로 접고
  "N개 매체 펼치기"로 매체를 노출 (10종 동시 집행 시 목록이 도배되던 문제).
  월간 셀의 기존 ×N 묶음(groupCellEvents)과 별개 구현 — 시트는 CalendarPage DaySheet rows,
  홈은 HomePage MediaToday group(). 펼친 항목 클릭 = 개별 상세 모달

## CSS 변수 사고 ('26.8.8 — 버튼이 통째로 안 보였음)
- **정의되지 않은 `var(--x)`는 그 속성을 통째로 무효로 만든다** (invalid at computed-value
  time). `background:var(--없는이름)`이면 배경이 **투명**이 되고, 흰 글씨가 흰 패널 위에
  찍혀 버튼이 사라진다. 앞 선언(`background:var(--green)`)으로 되돌아가지 않는다
- 실제 사고: 빨강 계열 토큰 이름을 `--clay`로 착각해 21곳에 썼는데 정의된 이름은 `--red`.
  회의록 "녹음 종료" 버튼이 안 보여 사용자가 "종료가 없다"고 리포트. 라운지·내 일정의
  경고색 21곳이 모두 같은 상태였다
- 같은 검사에서 **월간 리포트 덱 6곳이 폐기된 형광 옐로우그린 #EDFF5B를 하드코딩**으로
  들고 있던 것도 발견 ('26.8.4 스프링그린 전환 때 토큰을 안 써서 안 바뀌었음) → `var(--hl)`로 교체
- **회귀 감시 = test-data 19번** — index.css의 모든 `var()`를 :root 선언과 대조.
  폴백이 있는 `var(--x, 값)`과 JSX가 인라인 style로 넣는 변수는 제외

## 문자열 규칙 ('26.7.29 사용자 지시 — 전 화면 적용 완료, 신규 문구도 반드시 준수)
- UI 문자열(버튼·라벨·탭·헤더·배지·빈 상태·1문장 설명)에 **마침표 없음**
- **2문장 이상 서술 문단만** 마침표 사용, 한 화면에서 혼용 금지
- **em dash(—) 금지** — 부제는 줄 분리(`<br>`·별도 요소), 부연은 별도 요소, 접속은 문장 분리·쉼표
- **중점(·)·화살표(→)·말줄임표(…)·나열 슬래시·라벨 뒤 콜론 금지**
  (나열은 쉼표 또는 "A와 B", 빈 값은 `'—'`이 아니라 빈 문자열)
- 정보 위계는 기호가 아니라 **크기·굵기·줄바꿈**으로 (예: 모달 채널 뒤 세부 = `.md-ch-sub`
  별도 span / 공유 리포트 하이라이트 = 라벨·값 3열 구조 `.shr-pt-k`·`.shr-pt-v`)
- 적용 범위: src 전체 jsx·lib·data + media.js 스펙 텍스트. **구분자 변경 파급 주의** —
  매체명 "인스타그램 · 공식" → "인스타그램, 공식"으로 바뀌며 specLink.js·specMetrics.js
  매핑 키와 SpecLibrary splitMediaName 구분자를 함께 갱신했고, slot.kind "이미지·영상" →
  "이미지, 영상"이라 test-data 화이트리스트도 갱신함. 계정 그룹 '콘텐츠·IP' → '콘텐츠 IP'
  (accounts.mjs·MonitorPage·수집 데이터 동시). 구형 DB 저장분(rmn option 등)은 표시 시점에
  정규화(`plain()`)
- 검사: `document.body.innerText`에 · — → … 가 하나도 없어야 함 (전 탭·미러·외부·공유 리포트
  런타임 스캔으로 검증)

## 디자인 규칙 (v2 '26.7.29 전면 개편 — 위반 시 재작업, 반드시 준수)
- 폰트: **Wanted Sans Variable 단일** (`public/fonts/WantedSansVariable.woff2` 셀프호스트,
  index.css @font-face — jsdelivr CDN은 프록시 403 이력으로 셀프호스트 확정). Pretendard는 폴백만
- 컬러: **THE HYUNDAI 카드 그린 #0B4336 앵커** + 그린 틴트 그레이 스케일(--g50~--g900,
  --ink:#132019 — 순수 블랙·순수 그레이 미사용). **하이라이트 강조색 `--hl` = 스프링그린
  #8FEFAE** ('26.8.4 재보정 — 구 형광 옐로우그린 #EDFF5B는 잉크 텍스트 대비는 강했으나
  브랜드 그린·라일락 옆에서 하이라이터처럼 튐, 사용자 지시로 그린 계열 파스텔로 교체.
  잉크 대비 약 13.5:1로 가독성은 유지, 검색 매치·활성 탭 밑줄·칩·차트 최댓값·컨페티 등
  강조·매치 전용 용도는 불변 — index.css 토큰 하나만 바뀌면 전체 반영, 색상값 자체 임의
  변경 금지). **HYUNDAI Lilac(Pantone 9023U · #F0E4ED) 계열이 세컨더리 액센트** ('26.7.29 골드 전면 대체 —
  브랜드 컬러 스킴 기준, 사용자 지시): --lilac(공식·덱 다크 무대용) / --lilac-bar(#CFA3BD —
  밝은 배경 차트 비교군·할인율 바) / --lilac-ink(#96637F — 텍스트). 골드 #C9A96A 폐기
- **글래스모피즘**: --glass 토큰(반투명 백 + blur + --glass-border + 그린 틴트 --glass-shadow) —
  사이드바·캘린더 패널·그리드·모달·로그인 카드 공통. body = 앰비언트 라디얼 그라데이션 캔버스
  (fixed). **그라데이션 허용은 이 body 배경과 덱 무대뿐** — 컴포넌트 그라데이션은 여전히 금지
- **클로드 쿠세 금지**: 본제목 위 영문 대문자 eyebrow(자간 넓힌 소제목) 금지 — '26.7.29 전면 제거
- 빨강 #C4372C은 오직 금지사항·경고에만. 일반 강조에 빨강 금지
- 금지 패턴: border-left 컬러 스트립, pill 배지 남발, 이모지 아이콘, 과잉 border-radius
- 위계는 타이포그래피(크기·굵기 대비)로. 표는 가로선만
- 스펙 상세의 주인공은 규격 숫자 (대형 타이포, tabular-nums)

## 데이터 스키마 (현행)
```js
{
  group: "타겟형 매체" | "자사 매체" | "운영 매체",   // 섹션 구분
  cat: "아파트앱" | "APP" | "SNS" | ...,             // 필터 카테고리
  reg: "정기" | "비정기" | undefined,                 // 타겟형 전용
  name: "매체명",
  lead: "D-7 (영업일)",                               // 요청 리드타임
  verified: true | false,                             // 확정값 vs 가안
  target: "타겟·특성 1~2줄 (선택)",
  slots: [{ name, size, cap, fmt, note? }],           // 지면별 규격
  extra: { "심의": ..., "금지사항": ..., "담당": ..., "비고": ... },
  process: [{ d: "D-20", label: "...", hard? }],      // 진행 프로세스. hard: true = 전달 마감 필수 스텝 (형광 강조)
  src: "출처 표기"
}
```

## 데이터 소스와 신뢰도 (src 표기 기준)
1. **'26.2 타겟형 매체 운영 가이드라인** — 타겟형 10종. 확정값 (verified: true)
2. **'25.1 THE HYUNDAI Media Kit** — 자사 매체 12종. `data/media-kit-2025.md` 참조.
   '25년 기준이므로 verified: true + "'25.1 기준" 병기. 지표(팔로워 등)는 변동값이라
   스펙·프로세스만 사이트에 반영, 지표는 참고 필드로
3. **앱 구좌 신청서 (아이랩)** — `data/app-guja-request.md` 참조. 실제 신청 프로세스 확정값
4. **초기 가안 12종** — verified: false, "검증 전 가안" 표기 유지. 미디어키트 데이터와
   겹치는 항목(카카오톡, 인스타그램, 유튜브, APT LCD, 신문 등)은 미디어키트 값으로 대체할 것

## 우선 작업 순서 (TODO)
1. ~~`data/media-kit-2025.md`의 자사 매체 데이터를 media.js에 통합~~ — **완료 ('26.7)**
   - 자사 매체 12종 등재 (cat: 온드/언드/페이드 미디어). 겹치는 가안 7종 삭제
     (APT LCD, 앱 메인 배너, 앱 스플래시, 카카오톡 채널 메시지, 인스타 릴스·피드, 유튜브, 신문 15단)
   - '26.7 추가 정리: 출처 표기 전면 삭제 · 티스토리 블로그·페이스북 항목 삭제 ·
     콜키지프리 → 와지트 개명 반영
   - '26.7 설명문 삭제: GROUP_NOTES(그룹 상단 배너 — 연간 제휴 안내·미디어키트 출처문)와
     TARGET_COMMON(타겟형 공통 필드 — 요청 경로·운영 조건·할인·담당) 전면 비움(`{}`).
     "비고"(제작 별도 문의: 커뮤니케이션팀) 필드도 '26.7 사용자 요청으로 전면 삭제.
     스펙 라이브러리 푸터도 정리 — "문의: 미디어콘텐츠팀" 단일 문구(담당자 개인명·기준일 제거)
   - **'26.7.29 텍스트 검수**: extra "담당" 필드 20개 전면 삭제(외부·내부 공통).
     **참고 지표 자동 최신화** — `src/lib/specMetrics.js` withLiveMetrics가 언드 미디어
     7항목(인스타 공식·도시·에딧뎁트, 유튜브 공식·룸넘버·이야호, 와지트 IG+YT)의 지표를
     모니터링 수집분(instagram.js·youtube.js)으로 대체 + "(수집일)" 병기. 매핑에 없는
     매체(홈페이지·앱·카톡 등)와 수집값 없는 계정은 media.js 고정값 유지.
     계정 추가 시 LINK 상수에 한 줄. 탭 설명 문구도 사용자 지정본으로 교체
     (팀 일정·매체 캘린더·촬영·모니터링「기준: 최근 1개월·수집일」·RMN「부킹·재고·정산 관리」)
2. ~~진행 프로세스(D-day 타임라인) 필드 추가 + 타임라인 렌더링~~ — **완료 ('26.7)**
   - 타이포 기반 가로 타임라인 (원형 아님). 전달 마감 필수 스텝은 `hard: true` → 형광 강조
3. ~~앱 구좌 2종을 "자사 매체 > APP" 하위 지면으로 등재~~ — **완료 ('26.7)**
   - 구좌별 이미지 규격은 신청서 미기재 → "규격 확인 중" 표기. 이수정·백채은 선임 확인 후 갱신
4. 남는 가안 5종 — **부분 진행 ('26.7)**
   - ~~아파트 LCD: 실측 사이즈 3종(1080×1920·2460·2560) 슬롯 추가~~ — 완료
   - ~~더현대서울 외벽 LED → "지점 옥외·실내 사이니지"로 개편, 지점·위치별 실측
     사이즈 10개 슬롯(신촌·더현서·천호·무역 등) 반영~~ — 완료 (파일 용량·포맷은 확인 중,
     verified: false 유지)
   - ~~서울버스TV: 소재 제작 가이드 기준 규격 갱신(1920×540, 100MB 이하, 15/20/30초)~~ — 완료
   - ~~H.Point 메인 배너 · 친구톡 와이드: 소재 규격 삭제("규격 없음")~~ — 완료
   - 고지물(PMS)은 가안 유지 — 파트 확인 후 갱신
5. ~~외부 공유용 뷰: `?view=external` 쿼리 분기~~ — **완료 ('26.7)**
   - "참고 지표"(팔로워·MAU 등)는 항목째 숨김, "담당"의 개인명은 "미디어콘텐츠팀"으로 치환
     (App.jsx `sanitizeExtras`/`sanitizeGroupNote`), 헤더·푸터도 외부용 문구로 전환
6. ~~Vercel 배포~~ — **완료**. https://mediacontent-cal.vercel.app (main push = 자동 배포)
7. ~~매체 일정 캘린더~~ — **완료 ('26.7)**. 위 "캘린더 구조" 참조
8. ~~Supabase 연동~~ — **완료**. `src/config.js`에 키 입력됨 (팀 공유 DB + 로그인 게이트 활성)
9. ~~`src/data/team.js` 팀원 명단 채우기~~ — **완료 ('26.7, 20명)**. 인원 변동 시 한 줄 수정
10. ~~월간 집행 리포트~~ — **구현 완료 ('26.7.31, 시안 승인 후)**. 어드민 4번째 항목
    "월간 운영 리포트" = 16:9 덱 12장(채널 덱 언어 — 딥 그린 무대·히어로 1개·형광은
    최대값과 번호 전용·라일락 강조). **수치 = `src/lib/reportDeck.js` 전량 코드 계산**
    (report.js + YTA 월 버킷 + trend + UGC + 타겟APP + RMN + 전월 비교, 데이터 없는
    슬라이드 자동 생략 — 날조 없음), **서술 = `api/report-narrate.js`** (Vercel 함수,
    claude-opus-5, digest 문자열의 숫자만 인용 가능 — `checkNarration`이 서술 속 미확인
    숫자를 경고 배지로. 회당 약 $0.1). 문단별 "다시 쓰기", 저장 = `monthly_reports`
    스냅샷(수치 동결, **사용자 액션: data/report-setup.sql 1회 — setup.md 13장** +
    **Vercel 환경변수 ANTHROPIC_API_KEY**), 인쇄 = window.print(슬라이드당 1페이지).
    test-report.mjs가 덱 계산·서술 검증 회귀 감시. 내부 전용(어드민 게이트)
11. AI 어시스턴트 — 설계 확정('26.7): `docs/ai-assistant-design.md` + 비활성 스캐폴드
    `api/assistant.js`. **구현은 Opus 세션 예약** (화면 노출 현재 0)

## 도메인 지식 (혼동 주의)
- **APT LCD ≠ 아파트앱**: APT LCD는 엘리베이터 영상 매체(포커스미디어/KT타운보드),
  아파트앱은 타겟형 제휴 앱(아파트아이·아파트너·바이비). 사이트에서 경고 유지
- 예산·비용 표기는 천 원 단위 (40,000천 원)
- 지점 사고 사례 언급 시 "모 지점" 익명화
- 주류 관련: APT LCD는 주류(와인) 이미지 게첨 불가 / 데일리샷·Meta는 표현 검수 필수
- 사내 파일은 DRM(DRM-ONE) 걸려 있는 경우 많음 — 파싱 실패 시 사용자에게 해제본 요청

## 매체 캘린더 채널 — H.Point 승격 ('26.8.1)
- **H.Point는 기타의 세부(sub)가 아니라 독립 채널**로 승격 (`channels.js` CHANNELS 9번째
  항목, color `#6B4FA0`). 이전에는 `기타|H.Point`로만 표시돼 회색 "기타" 아이콘을 같이
  썼으나, 채널 아이콘은 항상 최상위 채널 id로만 그려지므로(sub 단위 아이콘 없음)
  구분이 안 됐음 — 승격해야만 고유 색·아이콘이 붙음
- **아이콘은 라인 글리프 원칙의 의도적 예외** — 알파벳 "H" 스트로크 마크
  (`ChannelIcon.jsx`, 사용자 지시 "로고는 H로 만들면 되겠다"). 다른 채널은 전부 추상 도형
- 빠른 입력 키워드(H.Point·에이치포인트·**에포**·h포인트·hpoint) → 이제 최상위 채널로 매핑,
  제목에서 "H.Point" 자동 제거(TITLE_STRIP). 한글 축약형(에포·에이치포인트)은 TITLE_ALIASES에도
  추가해 제목 표기를 "H.Point"로 정규화한 뒤 제거 — 안 그러면 "에포 리사이클 캠페인"처럼
  축약형이 제목에 그대로 남음('26.8 추가, test-parse 회귀 감시). MEDIA_RANK 2등급(백화점APP·
  카카오톡·기타와 동급), specLink.js 매핑 키도 `H.Point|`로 갱신 (media.js "H.Point 메인 배너" 그대로 연결)
- 매체 스펙 라이브러리의 "H.Point 메인 배너"(자사 매체 항목, `cat:"WEB, H.Point"`)는
  불변 — 이번 변경은 캘린더 채널 체계만 해당, 스펙 데이터 구조는 그대로
