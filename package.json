{
  "name": "media-spec-library",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sns:collect": "node scripts/sns/scrape-instagram.mjs && node scripts/sns/clean-instagram.mjs && node scripts/sns/scrape-youtube.mjs && node scripts/sns/clean-youtube.mjs",
    "sns:clean": "node scripts/sns/clean-instagram.mjs && node scripts/sns/clean-youtube.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
